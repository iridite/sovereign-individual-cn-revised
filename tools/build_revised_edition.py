from __future__ import annotations

import re
import shutil
import subprocess
import sys
import zipfile
from pathlib import Path
from textwrap import dedent
from xml.etree import ElementTree as ET

import fitz


ROOT = Path(__file__).resolve().parents[1]
LEGACY_SOURCE_ROOT = ROOT / "derived" / "clean-source" / "primary"
REVISED_ROOT = ROOT / "derived" / "revised-edition"
SOURCE_ROOT = REVISED_ROOT / "source"
MAIN_ROOT = SOURCE_ROOT / "main"
SUPPLEMENT_ROOT = SOURCE_ROOT / "supplements"
REVIEW_ROOT = REVISED_ROOT / "review"
BUILD_ROOT = REVISED_ROOT / "build"
STAGING_ROOT = BUILD_ROOT / "staging"
ASSET_ROOT = BUILD_ROOT / "assets"
OUTPUT_ROOT = BUILD_ROOT / "output"
REPORT_ROOT = BUILD_ROOT / "reports"
RAW_UNPACKED_ROOT = BUILD_ROOT / "raw-unpacked"

PDF_COVER_SOURCE = ROOT / "tex-zh" / "versions" / "Sovereign_Individual_V2.pdf"
ALT_COVER_SOURCE = Path(r"C:\Users\ollama\Downloads\the-sovereign-individual-9781797103389_hr.jpg")

TITLEPAGE_PATH = MAIN_ROOT / "00-titlepage.md"
IMPRINT_PATH = MAIN_ROOT / "01-imprint.md"
STYLE_PATH = SOURCE_ROOT / "style.css"
METADATA_PATH = SOURCE_ROOT / "metadata.yaml"
MAIN_BOOK_PATH = SOURCE_ROOT / "book-main.md"
SUPPLEMENT_BOOK_PATH = SOURCE_ROOT / "supplements.md"
NOTES_REMOVED_PATH = SUPPLEMENT_ROOT / "notes-removed.md"
SUPPLEMENT_NOTE_PATH = SUPPLEMENT_ROOT / "version-note.md"
APPENDIX1_PATH = MAIN_ROOT / "15-appendix.md"
REVIEW_README_PATH = REVIEW_ROOT / "README.md"
BOUNDARY_PATH = REVIEW_ROOT / "boundary-notes.md"
ISSUE_CLOSURE_PATH = REVIEW_ROOT / "issue-closure.md"
TERMINOLOGY_PATH = REVIEW_ROOT / "terminology.md"

RAW_EPUB = OUTPUT_ROOT / "主权个人-修订版-raw.epub"
FINAL_EPUB = OUTPUT_ROOT / "主权个人-修订版.epub"
LOG_PATH = REPORT_ROOT / "build.log"
REPORT_PATH = REPORT_ROOT / "validation-report.md"
COVER_PATH = ASSET_ROOT / "cover.png"
BOOK_COVER_RATIO = 2 / 3

XHTML_NS = "http://www.w3.org/1999/xhtml"
EPUB_NS = "http://www.idpf.org/2007/ops"
OPF_NS = "http://www.idpf.org/2007/opf"
NCX_NS = "http://www.daisy.org/z3986/2005/ncx/"
NAMESPACES = {
    "xhtml": XHTML_NS,
    "epub": EPUB_NS,
    "opf": OPF_NS,
    "ncx": NCX_NS,
}

ET.register_namespace("", XHTML_NS)
ET.register_namespace("epub", EPUB_NS)
ET.register_namespace("opf", OPF_NS)

MAIN_FILE_ORDER = [
    "00-titlepage.md",
    "01-imprint.md",
    "01-preface.md",
    "02-preface2.md",
    "03-chapter1.md",
    "04-chapter2.md",
    "05-chapter3.md",
    "06-chapter4.md",
    "07-chapter5.md",
    "08-chapter6.md",
    "09-chapter7.md",
    "10-chapter8.md",
    "11-chapter9.md",
    "12-chapter10.md",
    "13-chapter11.md",
    "14-afterword.md",
    "15-appendix.md",
    "16-appendix2.md",
]

COPIED_MAIN_FILES = [
    "01-preface.md",
    "02-preface2.md",
    "03-chapter1.md",
    "04-chapter2.md",
    "05-chapter3.md",
    "06-chapter4.md",
    "07-chapter5.md",
    "08-chapter6.md",
    "09-chapter7.md",
    "10-chapter8.md",
    "11-chapter9.md",
    "12-chapter10.md",
    "13-chapter11.md",
    "14-afterword.md",
    "15-appendix.md",
    "16-appendix2.md",
]

TITLEPAGE_TEXT = dedent(
    """\
    # 主权个人

    <div class="titlepage">
      <p class="tp-subtitle">掌握信息时代的变革</p>
      <p class="tp-original"><em>The Sovereign Individual</em></p>
      <p class="tp-authors">James Dale Davidson<br />Lord William Rees-Mogg</p>
      <p class="tp-translator">陈三省 译</p>
      <p class="tp-edition">1997 年初版 · 2020 年再版</p>
    </div>
    """
)

IMPRINT_TEXT = dedent(
    """\
    # 版权与编目信息

    <div class="imprint-page">
    <div class="imprint-block">
    <p class="imprint-line">书名：主权个人</p>
    <p class="imprint-line">作者：James Dale Davidson，Lord William Rees-Mogg</p>
    <p class="imprint-line">译者：陈三省</p>
    <p class="imprint-line">出版信息：筷子小手出版社，2025.09</p>
    <p class="imprint-line">丛书：加密未来系列丛书</p>
    <p class="imprint-line">ISBN：978-80-7340-097-2</p>
    <p class="imprint-line">中国版本图书馆 CIP 数据核字：2025 第 00000613 号</p>
    </div>

    <div class="imprint-block">
    <p class="imprint-line">原书版本：1997 年第一版，2020 年第二版</p>
    <p class="imprint-line">当前版本：基于开源 LaTeX 源稿与逐章审校台账整理的修订版电子书</p>
    <p class="imprint-line">版本口径：正文精修版；保留正式前置页、后记与两份附录</p>
    <p class="imprint-line">本版在陈三省译稿基础上经二次复校修订：iridite</p>
    <p class="imprint-line">使用说明：仅供个人阅读与学习交流使用，请勿商用</p>
    </div>

    <div class="imprint-block">
    <p class="imprint-line">责任编辑：陈三省</p>
    <p class="imprint-line">责任校对：李不乖</p>
    <p class="imprint-line">责任印刷：筷子小手</p>
    <p class="imprint-line">封面设计：烟云幻梦</p>
    <p class="imprint-line">仓库地址：github.com/Macin20/sovereign-individual-cn</p>
    </div>
    </div>
    """
)

METADATA_TEXT = dedent(
    """\
    title: "主权个人"
    author:
      - "James Dale Davidson / Lord William Rees-Mogg"
    publisher: "Springer"
    lang: "zh-CN"
    rights: "仅供学习交流使用，请勿商用。"
    identifier: "sovereign-individual-cn-revised"
    description: "《主权个人》中文修订版。基于开源 LaTeX 中文源稿、逐章中英对照审校记录与二次复核补遗整理，面向微信读书等可重排阅读器，强调正文边界清晰、脚注克制、目录稳定与译风统一。"
    """
)

STYLE_TEXT = (ROOT / "epub" / "style.css").read_text(encoding="utf-8")

TEXT_REPLACEMENTS: dict[str, list[tuple[str, str]]] = {
    "01-preface.md": [
        (
            "只有这种特有的、长期的警惕意识，才使得党的领导人能在本书分析的趋势中获得胜利。",
            "只有这种独特而长期的警觉，才使得党的领导人能在本书所分析的趋势中占得上风。",
        ),
        (
            "在 2020 年，阅读《主权个人》，是你认真思考自己的行动将塑造何种未来的一种方式，是一次不容浪费的学习机会。",
            "在 2020 年，重读《主权个人》，正是认真思考“你的行动将塑造怎样的未来”的一种方式，是一次不容浪费的学习机会。",
        ),
    ],
    "02-preface2.md": [
        ("2023年 GhatGPT 和其他 AI 翻译工具已经非常成熟了", "2023年 ChatGPT 和其他 AI 翻译工具已经非常成熟了"),
    ],
    "03-chapter1.md": [
        ("基督的敌人", "敌基督"),
        ("“屁股决定立场”", "“立场往往取决于所处的位置”"),
        ("国库资产负债表上一个事实的项目", "国库资产负债表上的一个项目"),
        ("“集体无意识”的鉴别大师", "“集体无意识”理论的代表人物"),
        ("“同类匹配”", "“同类聚合”"),
        ("任何地29方", "任何地方"),
        ("“元宇宙”(mataverse)", "“元宇宙”（metaverse）"),
        ("公民意识将走向骑士精神的道路。", "公民观念也将步上骑士精神当年的命运。"),
        ("所有的一切，都将因 2000年的到来而画上句号。", "所有的一切，都将因 2000 年的到来而画上句号。"),
        ("20世纪支撑起来的民族国家的公民神话", "20 世纪所支撑起来的民族国家公民神话"),
        ("到2025 年", "到 2025 年"),
        ("前15%的人口", "前 15% 的人口"),
        ("1000美元", "1000 美元"),
        ("税收能力将骤降 50-70%", "税收能力将骤降 50% 至 70%"),
        ("公元2000年", "公元 2000 年"),
        ("当Hallerith牌打卡机只能容纳80个字符时", "当 Hallerith 牌打卡机只能容纳 80 个字符时"),
        ("埋伏了40年", "埋伏了 40 年"),
        ("在1997年2月7日发布了", "在 1997 年 2 月 7 日发布了"),
        (
            "我们相信，个人经济主权的时代即将到来。正如曾经被“国有化”的钢铁厂、电话公司、矿山和铁路，在世界各地被迅速私有化一样，很快你将看到私有化的终极形式——个人的全面非国有化。在新的千年，主权个人不再是国家的资产，不再是国库资产负债表上一个事实的项目。在 2000 年的转折之后，非国有化的公民，不再是我们所熟知的公民，而是政府的客户。",
            "我们相信，个人经济主权的时代即将到来。正如曾经被“国有化”的钢铁厂、电话公司、矿山和铁路，后来在世界各地被迅速私有化一样，很快你将看到私有化的终极形式——个人的全面去国家化。在新的千年里，主权个人不再是国家的资产，也不再只是国库资产负债表上的一个项目。在 2000 年的转折之后，完成去国家化的个人，不再是我们所熟知的公民，而是政府的客户。",
        ),
        (
            "就像封建主义崩溃之后的骑士誓言一样，民族国家针对公民身份的各种条款和条件，在主权商业化之后将会过时。在与国家强权的关系中，21 世纪的主权个人不再是一个被征税的公民，而是在一种“新型逻辑空间”运作下的政府的客户。他们将为自己所需要的、最低限度的政府讨价还价，并按照合同付款。与几个世纪以来人们所习惯的不同，信息时代的政府，将通过新的原则组织起来。一些司法管辖区和主权服务，将建立在一种“同类匹配”的系统上；在这个系统中，亲密关系，包括商业好感，是虚拟管辖区获得忠诚的基础。",
            "就像封建主义崩溃后骑士誓言变得过时一样，民族国家围绕公民身份设下的种种条款与条件，也会在主权商业化的时代失效。在与国家强权的关系中，21 世纪的主权个人不再只是一个被征税的公民，而是运行于一种“新型逻辑空间”中的政府客户。他们会为自己所需的、最低限度的政府服务讨价还价，并按合同付费。与几个世纪以来人们所熟悉的情形不同，信息时代的政府将按照新的原则组织起来。一些管辖区和主权服务，会建立在一种“亲和性匹配”的机制之上；在这种机制中，亲密关系乃至商业上的好感，都会成为虚拟管辖区赢得忠诚的基础。",
        ),
        (
            "就像封建主义崩溃之后的骑士誓言一样，民族国家针对公民身份的各种条款和条件，在主权商业化之后将会过时。在与国家强权的关系中，21 世纪的主权个人不再是一个被征税的公民，而是在一种“新型逻辑空间”运作下的政府的客户。他们将为自己所需要的、最低限度的政府讨价还价，并按照合同付款。与几个世纪以来人们所习惯的不同，信息时代的政府，将通过新的原则组织起来。一些司法管辖区和主权服务，将建立在一种“同类聚合”的系统上；在这个系统中，亲密关系，包括商业好感，是虚拟管辖区获得忠诚的基础。",
            "就像封建主义崩溃后骑士誓言变得过时一样，民族国家围绕公民身份设下的种种条款与条件，也会在主权商业化的时代失效。在与国家强权的关系中，21 世纪的主权个人不再只是一个被征税的公民，而是运行于一种“新型逻辑空间”中的政府客户。他们会为自己所需的、最低限度的政府服务讨价还价，并按合同付费。与几个世纪以来人们所熟悉的情形不同，信息时代的政府将按照新的原则组织起来。一些管辖区和主权服务，会建立在一种“亲和性匹配”的机制之上；在这种机制中，亲密关系乃至商业上的好感，都会成为虚拟管辖区赢得忠诚的基础。",
        ),
        ("21 世纪的主权个人不再是一个被征税的公民，而是在一种“新型逻辑空间”运作下的政府的客户。", "21 世纪的主权个人不再只是一个被征税的公民，而是运行于一种“新型逻辑空间”中的政府客户。"),
        ("一些司法管辖区和主权服务，将建立在一种“同类聚合”的系统上；在这个系统中，亲密关系，包括商业好感，是虚拟管辖区获得忠诚的基础。", "一些管辖区和主权服务，会建立在一种“亲和性匹配”的机制之上；在这种机制中，亲密关系乃至商业上的好感，都会成为虚拟管辖区赢得忠诚的基础。"),
        (
            "在未来，怎样描述主权个人之间，以及他们和残留的政府之间的关系，可能需要一套新的道德词汇。我们猜测，当这些描述新型关系的术语成为社会的焦点时，很多来自 20 世纪民族国家“公民”时代的人，会非常生气。国家的终结和“个人的非国有化”，将使一些曾经被热烈拥抱的理念，变得没有市场；如“法律面前人人平等”，因为这些理念的前提是即将被淘汰的权力关系。",
            "在未来，怎样描述主权个人之间，以及他们与残存政府之间的关系，可能需要一套新的道德词汇。我们猜测，当这些描述新型关系的术语成为社会焦点时，许多来自 20 世纪民族国家“公民”时代的人都会十分愤怒。国家的终结与“个人的去国家化”，将使一些曾经被热烈拥抱的理念失去市场；比如“法律面前人人平等”，因为这些理念的前提正是即将被淘汰的权力关系。",
        ),
        (
            "我们相信，个人经济主权的时代即将到来。正如曾经被“国有化”的钢铁厂、电话公司、矿山和铁路，在世界各地被迅速私有化一样，很快你将看到私有化的终极形式——个人的全面非国有化。在新的千年，主权个人不再是国家的资产，不再是国库资产负债表上的一个项目。在 2000 年的转折之后，非国有化的公民，不再是我们所熟知的公民，而是政府的客户。",
            "我们相信，个人经济主权的时代即将到来。正如曾经被“国有化”的钢铁厂、电话公司、矿山和铁路，后来在世界各地被迅速私有化一样，很快你将看到私有化的终极形式——个人的全面去国家化。在新的千年里，主权个人不再是国家的资产，也不再只是国库资产负债表上的一个项目。在 2000 年的转折之后，完成去国家化的个人，不再是我们所熟知的公民，而是政府的客户。",
        ),
        ("搞笑且不合时宜。", "显得可笑而不合时宜。"),
        ("可能再次发生 1929年式的股灾。", "可能再次发生 1929 年式的股灾。"),
        ("然后短短 6 个月内，1987年 10 月", "然而短短 6 个月后，1987 年 10 月"),
        ("2000年的变革", "2000 年的变革"),
        ("30年代的水平", "30 年代的水平"),
        ("在20世纪四五十年代", "在 20 世纪四五十年代"),
        ("1949年出版", "1949 年出版"),
        ("1944年出版", "1944 年出版"),
        ("1934年说", "1934 年说"),
        ("1999 年 8 月 18日", "1999 年 8 月 18 日"),
        ("1492年哥伦布发现美洲", "1492 年哥伦布发现美洲"),
        ("中世纪的500年里", "中世纪的 500 年里"),
        ("然后，在15世纪的最后十年", "然后，在 15 世纪的最后十年"),
        ("出版于1805年", "出版于 1805 年"),
        ("他在20世纪四五十年代", "他在 20 世纪四五十年代"),
        ("把它读成 1900年。", "把它读成 1900 年。"),
        ("让联邦计算机为2000年做好准备", "让联邦计算机为 2000 年做好准备"),
        ("85 后的今天", "85 年后的今天"),
        ("2000年并没有那么可怕", "2000 年并没有那么可怕"),
        ("大多数的当代组织，在新的千年里，在很大程度上都很难或者不可能存在。", "大多数当代组织，在新的千年里都将难以为继，甚至不复存在。"),
        ("到1995 年的 6 月底", "到 1995 年 6 月底"),
        ("通货膨胀将在很大程度上别摒除", "通货膨胀将在很大程度上被摒除"),
        ("私有市场控制的网络货币", "由私人市场主导的网络货币"),
        ("在未来，怎样描述主权个人之间，以及他们与残存政府之间的关系，可能需要一套新的道德词汇。", "在未来，主权个人彼此之间，以及他们与残存政府之间的关系，或许需要一套新的道德词汇来加以描述。"),
        ("随着虚拟社区的凝集力越来越强", "随着虚拟社区的凝聚力越来越强"),
        ("在新的千年里，这种实体将重新崛起，代替苟延残喘的民族国家，在不安的世界提供保护，促进契约的履行。", "在新的千年里，这类实体将重新兴起，取代苟延残喘的民族国家，在动荡的世界里提供保护并促成契约履行。"),
    ],
    "04-chapter2.md": [
        ("在公元 6 年的 9 月", "在公元 476 年的 9 月"),
        ("安东尼瘟疫", "安敦宁瘟疫"),
        ("尿毒症", "流感"),
        ("讨论罗马的衰落，简直大逆不道", "声称罗马已经灭亡，简直荒唐可笑"),
        ("马尔切利努斯伯爵（CountMarcellinus）", "马尔切利努斯伯爵（Count Marcellinus）"),
        (
            "当占据权力主导地位的机构被迫讨价还价，去强化一个对它们有利的结论时",
            "当主导性制度的权力被卷入其中，用来强化某个方便而有利的结论时",
        ),
        ("他只作为一个西罗马的贵族，帮助芝诺管理意大利。", "他自己则只以芝诺麾下“帕特里修斯”的身份代治意大利。"),
        ("在你思考当下的世界时，参考罗马衰亡的远鉴，是很有必要的，其中有诸多关联之处。", "罗马衰亡这个久远的例子，与我们思考当今世界有多重相关性。"),
        ("大都数探讨未来的书", "大多数探讨未来的书"),
        ("安东尼时期的瘟疫", "安敦宁瘟疫"),
        ("微处理技术，将溶解砖头中的砂浆", "微处理技术会把这堵墙里的灰浆一点点化开"),
        ("技术有几 个关键的维度", "技术有几个关键维度"),
        ("在1276 年，没人提出过“人人生而平的”", "在 1276 年，没人提出过“人人生而平等”"),
        (
            "我们知道，微寄生虫不可能不继续对现代人类社会造成破坏。",
            "我们看不出有什么理由能说明，微寄生虫不会继续对现代人类社会大肆破坏。",
        ),
    ],
    "05-chapter3.md": [
        ("“Allod”，即自主地", "“Allod”，即自由保有地产（freehold property）"),
        ("昂贵的马马和盔甲", "昂贵的战马和盔甲"),
        (
            "此外，提升骑士作战效率的发明还有，方便骑士挥舞重武器的马鞍、马刺，以及用一直手就能控制马匹的马鞍。",
            "此外，提升骑士作战效率的发明还有马刺，以及能让骑士在作战时单手控马的马嚼子（curb bit）。",
        ),
        ("排泄沼泽", "排干沼泽"),
        ("降雨或发水的神也往往是战争之神", "降雨与洪水之神，往往也就是战争之神"),
        ("伴随农耕而来的新生活方式是“逃避”（evodeviant）。", "伴随农耕而来的新生活方式，是一种“偏离进化适配的”生活（evodeviant）。"),
        ("庄家需要照料", "庄稼需要照料"),
        ("他们像无业游民一样，随心所欲地漂流", "他们像流浪汉那样，想到哪里就漂到哪里"),
        ("destriers 的大体型冲锋战马", "destriers 的大型冲锋战马"),
        ("一种叫做destriers 的大体型冲锋战马", "一种叫做 destriers 的大型冲锋战马"),
        ("是一件令人深感遗憾的事", "无疑是一种令人深感遗憾的转变"),
    ],
    "06-chapter4.md": [
        (
            "在我们写这段文字的时候，还没有明确的证据表明人们对政治的抗拒。它会在后面发生。",
            "目前还几乎看不到一种清晰成形、能够明确说出口的反政治立场；那要到后面才会出现。",
        ),
        ("大政治的环境以及发生了改变", "大政治的环境已经发生了改变"),
        ("16 手宽", "16 掌高"),
        ("应用“现代文本批评经文”奠定了基础", "为“把现代文本批评方法运用于《圣经》”奠定了基础"),
        ("对他们收取的财务管理极差。", "向人要的钱不少，提供的服务却很差。"),
        ("谋取暴力", "牟利"),
        ("听弥散", "听弥撒"),
        ("马丁·凡·克瑞福德", "马丁·范·克雷维尔德"),
        ("这就好像说一个人只要用空气中吸收营养就能活下去。", "这就好像说一个人只靠从空气中吸收营养就能活下去。"),
        ("在那是它好像是个贬义词", "在那时，它似乎还是个贬义词"),
        ("兜售上帝的恩典和罪孽的宽恕", "兜售上帝的恩典与罪过的赦免"),
        ("马丁·凡·克利福德", "马丁·范·克雷维尔德"),
        ("克利福德写道：", "范·克雷维尔德写道："),
        ("用克利福德的话说", "用范·克雷维尔德的话说"),
        ("克瑞福德教授著作等身", "克雷维尔德著作等身"),
        ("萨克森的选帝候弗雷德里克", "萨克森的选帝侯弗雷德里克"),
        ("绝不“从战场上撤退超过 4 英里。", "绝不“离开战场超过四英亩之地”。"),
        ("离开战场超过四英亩之地”。而不久之后，他们中的九十多人就因为这一誓言而送命。”", "离开战场超过四英亩之地”；不久之后，他们中的九十多人就因为这一誓言而送命。"),
        (
            "例如，星辰骑士团曾经发誓，绝不“离开战场超过四英亩之地”。而不久之后，他们中的九十多人就因为这一誓言而送命。”",
            "例如，星辰骑士团曾经发誓，绝不“离开战场超过四英亩之地”；不久之后，他们中的九十多人就因为这一誓言而送命。",
        ),
        (
            "在这些誓言中，常见的情况有：如果他们加入了只有一个人的锁链囚徒帮（one-person chain gang），就要遮住一只眼睛，只在站立的时候吃喝，并且要把自己搞残废，变成瘸子。",
            "这些誓言常见的形式，是给自己套上一条单人囚链（one-person chain gang）：遮住一只眼睛，只能站着吃喝，故意把自己折磨成瘸子。",
        ),
        ("反而把它推倒了最能发挥颠覆作用的地方。", "反而把它推到了最能发挥颠覆作用的地方。"),
        ("贫穷的地方神职人员，向人要的钱不少，提供的服务却很差。", "贫困的地方神职人员，向人要的钱不少，提供的服务却很差。"),
    ],
    "07-chapter5.md": [
        ("因为有灌溉设施，那里的农业产量更要。", "因为有灌溉设施，那里的农业生产率更高，土地所有者弃地逃亡的问题也更严重。"),
        ("民主主义是一项发明，使一个国家可以有效地扩大它的军事规模。", "民族主义是一项发明，它使一个国家能够更有效地扩大其军事动员的规模。"),
        ("以及 NFL 冰球运动员来自新的国家", "以及国家冰球联盟（NHL）的球员开始来自一些新的国家"),
        ("它是为了防止里面的 人逃跑", "它是为了防止里面的人逃跑"),
        ("对政府的控制有三种基本的选择：业主，雇员和客户", "政府控制权大体有三种归属：业主、雇员和客户"),
        ("哪些雇员群体可以被重要", "哪些雇员群体应该被裁掉"),
        ("很多人比少数人更容易对对", "很多人比少数人更容易对付"),
        ("大众民主和和福利国家能否长期生存", "大众民主和福利国家能否长期生存"),
        ("阿尔贝托·滕森（ALBERTO FUJIMORI）", "阿尔韦托·藤森（ALBERTO FUJIMORI）"),
        ("滕森关闭国会", "藤森关闭国会"),
        ("滕森要着手改变的", "藤森要着手改变的"),
        ("前滕森时代", "前藤森时代"),
        ("滕森要实施全面的改革计划", "藤森要实施全面的改革计划"),
        ("于是，滕森关闭了国会。", "于是，藤森关闭了国会。"),
        ("在滕森[^07-chapter5-fn-4]封锁国会之前", "在藤森[^07-chapter5-fn-4]封锁国会之前"),
        ("滕森指称秘鲁国会犹豫不决", "藤森指称秘鲁国会犹豫不决"),
        ("当滕森总统把国会送回老家时", "当藤森总统把国会送回老家时"),
        ("滕森找对了路", "藤森找对了路"),
        ("在这个还是那个司法管辖区做生意", "在这个还是那个管辖区做生意"),
        ("现代时期出现的统治机构，反应的是一个或几个世纪前的大政治状况。", "现代时期出现的统治机构，反映的是一个或几个世纪前的大政治状况。"),
    ],
    "08-chapter6.md": [
        ("你可以会指望", "你会指望"),
        ("环境种", "环境中"),
        ("数百个司法管辖区", "数百个管辖区"),
        ("最佳服务的司法管辖区", "最佳服务的管辖区"),
        ("交易的司法管辖区", "交易所在的管辖区"),
        ("国家的福利制度也会随之崩溃", "国家身份所附带的种种好处，也会随之瓦解"),
        ("一根细如发丝的光线每秒可以传输一万亿字节", "一根细如发丝的光纤，每秒可以传输一万亿比特"),
        ("正是在那个时候，乔治·吉尔德首次提出，带宽的复合增长速度甚至会超过微处理器的发展。", "而乔治·吉尔德早在 1993 年就首次提出，带宽的复合增长速度甚至会超过微处理器的发展。"),
        (
            "当这么强大的通信潜力被释放，将有大量的钱花在网络通信上，因为它会很便宜；而像专线电话和电视等既有媒体将被淘汰。",
            "一旦如此强大的通信能力被释放出来，大量资金就会流向网络通信，因为它的成本会极低；而专线电话、电视之类的既有媒介则会被淘汰。",
        ),
        (
            "如果带宽继续以每年三倍的速度增长，那么到 2012 年，网络带宽将是 1993 年的 10 亿倍；正是在那个时候，乔治·吉尔德首次提出，带宽的复合增长速度甚至会超过微处理器的发展。",
            "如果带宽继续以每年三倍的速度增长，那么到 2012 年，网络带宽将是 1993 年的 10 亿倍；而乔治·吉尔德早在 1993 年就首次提出，带宽的复合增长速度甚至会超过微处理器的发展。",
        ),
    ],
    "09-chapter7.md": [
        (
            "> 真正的问题在于控制。互联网无远弗届，任何一个政府都无法轻易控制。它将创造一个无缝链接的、不可监控的、反主权的全球经济区，并对民族国家的概念发出质疑。flushright —— 约翰·佩里·巴洛（JOHN BARLOW ） flushright",
            "> 真正的问题在于控制。互联网无远弗届，任何一个政府都无法轻易控制。它将创造一个无缝链接的、不可监控的、反主权的全球经济区，并对民族国家的概念发出质疑。\n>\n> —— 约翰·佩里·巴洛（JOHN PERRY BARLOW）",
        ),
        ("ARTHER C. CLARK", "ARTHUR C. CLARKE"),
        ("针对你的提醒进行量身定做", "根据从你电脑里扫描并经网络传输的照片，为你量身定做"),
        ("Punte del Este", "Punta del Este"),
        ("是锐感的还是钝感的", "是锐痛还是钝痛"),
        ("工业注意", "工业主义"),
        ("质素", "质数"),
        ("20 世纪通货膨胀的兴趣与世界权力的平衡密切相关", "20 世纪通货膨胀的兴衰与世界权力的平衡密切相关"),
        ("很多埃塞尔比亚人只有一只脚", "很多埃塞俄比亚人只有一只脚"),
        ("成本降会大幅降低", "成本会大幅降低"),
        ("你可以随心所欲额地跨越边界", "你可以随心所欲地跨越边界"),
        ("根据核磁共振图像现实的肿瘤大小和特征", "根据核磁共振图像显示出的肿瘤大小和特征"),
        ("在网络空间的新疆土内", "在网络空间这片新边疆上"),
        ("高投资回报率在某地地方更容易实现", "高投资回报率在某些地方更容易实现"),
        ("过去30 年", "过去 30 年"),
        ("实现20%以上的收益", "实现 20% 以上的收益"),
        ("在美国运作的一些共同基金", "美国一些共同基金"),
        ("3300多万美元", "3300 多万美元"),
        ("20世纪 80 年代", "20 世纪 80 年代"),
        ("司法管辖区", "管辖区"),
        ("平均年收益率只有10%。", "平均年收益率只有 10%。"),
        ("即使你连 10%的年化收益也做不到", "即使你连 10% 的年化收益也做不到"),
        (
            "如果经济学家关于利润最大化的假设是正确的——我们相信一般情况还是对的，那么你可以很有把握地预测，只要有机会大多数人都会采取行动去挽救 5500万美元的损失。我们也是这么想的。当复利的黑魔法在高税率国家的成功人士心中变得愈加清晰，他们就会在不同的司法管辖区之间进行对比选择，就像现在买汽车或者上保险一样。如果你对此表示怀疑，你可以到纽约或多伦多的大街上，随机拦住一些人，问他们会不会为了 5500 万美元搬到百慕大。如果经济学家的利润最大化假设是正确的，我们相信它们一般都是正确的，那么你可以做出的一个比较确定的预测是，如果可以的话大多数人都会采取行动来挽救 5500 万美元。这就是我们的预测。当复利的黑魔法在高税率国家的成功人士心中变得更加清晰时，他们就会开始认真地在不同的司法管辖区之间进行挑选，就像他们现在购买汽车或比较保险单的利率一样。如果你怀疑这一点，只需在纽约或多伦多的大街上随机拦住人们，问他们愿不愿意为了 5500 万美元搬到百慕大。答案不言自明。",
            "如果经济学家关于利润最大化的假设是正确的——我们相信通常如此——那么你几乎可以肯定地预测：只要有机会，大多数人都会采取行动，去挽回这 5500 万美元的损失。我们也是这么看的。当复利的黑魔法在高税率国家的成功人士心中变得愈发清晰，他们就会开始认真比较不同管辖区，就像今天比较汽车或保险一样。如果你对此表示怀疑，不妨到纽约或多伦多的街头随机拦住一些人，问他们是否愿意为了 5500 万美元搬到百慕大。答案几乎不言自明。",
        ),
        (
            "如果经济学家关于利润最大化的假设是正确的——我们相信一般情况还是对的，那么你可以很有把握地预测，只要有机会大多数人都会采取行动去挽救 5500万美元的损失。我们也是这么想的。当复利的黑魔法在高税率国家的成功人士心中变得愈加清晰，他们就会在不同的管辖区之间进行对比选择，就像现在买汽车或者上保险一样。如果你对此表示怀疑，你可以到纽约或多伦多的大街上，随机拦住一些人，问他们会不会为了 5500 万美元搬到百慕大。如果经济学家的利润最大化假设是正确的，我们相信它们一般都是正确的，那么你可以做出的一个比较确定的预测是，如果可以的话大多数人都会采取行动来挽救 5500 万美元。这就是我们的预测。当复利的黑魔法在高税率国家的成功人士心中变得更加清晰时，他们就会开始认真地在不同的管辖区之间进行挑选，就像他们现在购买汽车或比较保险单的利率一样。如果你怀疑这一点，只需在纽约或多伦多的大街上随机拦住人们，问他们愿不愿意为了 5500 万美元搬到百慕大。答案不言自明。这就好像问马克吐温，更愿意与赤身裸体的莉莉安·罗素共度良宵，还是与全身戎装的格兰特将军，他可没斟酌太久。",
            "如果经济学家关于利润最大化的假设是正确的——我们相信通常如此——那么你几乎可以肯定地预测：只要有机会，大多数人都会采取行动，去挽回这 5500 万美元的损失。我们也是这么看的。当复利的黑魔法在高税率国家的成功人士心中变得愈发清晰，他们就会开始认真比较不同管辖区，就像今天比较汽车或保险一样。如果你对此表示怀疑，不妨到纽约或多伦多的街头随机拦住一些人，问他们是否愿意为了 5500 万美元搬到百慕大。答案几乎不言自明。",
        ),
        ("它由数百位质数的加密序列组成", "它由数百位数质数构成的加密序列组成"),
        ("转移的是数百位质数的加密序列", "转移的是数百位数质数构成的加密序列"),
        ("要解开数百位质数的乘积是不可能的", "要解开数百位数质数的乘积几乎是不可能的"),
        ("在信息时代新的大政治条件下，切实可行货币制度的不是金本位这样的弱联系，而是一种强联系；", "在信息时代新的大政治条件下，真正切实可行的将不是金本位那样的弱联系，而是一种强联系；"),
        ("尝试将黄金再货币化", "尝试将黄金重新货币化"),
        ("根据从你电脑里扫描到并经网络传输的照片，根据从你电脑里扫描并经网络传输的照片，为你量身定做。", "根据从你电脑里扫描并经网络传输的照片，为你量身定做。"),
        ("会以高压水枪般的动力和速度转移到网络空间。", "将以一种近乎液压般的强大压力涌向网络空间。"),
        ("我们也是这么看的。", "我们也正是这样判断的。"),
        ("只需要 55 美元，而不是 5500 万美元，网络经济的参与者就可以获得比工业时期或历史任何时期更好的资产保护。", "网络经济的参与者只需付出 55 美元，而不是承受 5500 万美元的损失，就可以获得比工业时期乃至历史上任何时期都更好的资产保护。"),
    ],
    "10-chapter8.md": [
        ("中央银行的高负债率", "中央政府的高负债率"),
        ("越来越多的加拿大邦，回归到亚当·斯密", "越来越多的加拿大各省，会回归到亚当·斯密"),
        ("### 苍白之地的非公民们", "### 帕勒地区的非公民们"),
        ("“苍白之地的公民”（the citizens of the Pale）", "“帕勒地区的公民”（the citizens of the Pale）"),
        ("”的粉丝肯定就不会。", "”的拥趸肯定就不会。"),
        ("他们就像猪一样蠢。", "他们蠢得就像猪嘴边滴下来的口水。"),
        (
            "可能跟扔出八个六差不多。然而，阿蒙指出，如果在这些素质中得分有高有低，可能会产生“天赋不平衡、不和谐的人，尽管他们在某些方面非常出色，但无法成功应对生活的考验。”。",
            "可能和掷出八个六差不多。然而，阿蒙指出，如果这些素质高低失衡，就会出现“天赋不平衡、不和谐的人；尽管他们在某些方面非常出色，却无法成功应对生活的考验”。",
        ),
        ("用萝卜来比喻社会金字塔是说得通的，因为像在现代工业社会中，它的多数群体是处于中间位置", "用萝卜来比喻社会结构是说得通的，因为在现代工业社会中，多数人处于中间位置"),
        ("查尔斯·布斯所说“积极社会效用的最低限度”", "查尔斯·布斯所说“最低限度的正面社会效用”"),
        ("在其此间", "在此期间"),
        ("周边的相关收入都会提高", "各方面收入普遍上升"),
        ("曼瑟·奥尔森（MANCOR OLSON）", "曼瑟·奥尔森（MANCUR OLSON）"),
        ("熟练工人和资本会涌入到这些地区，直到收益区域平稳。", "熟练工人和资本会涌入这些地区，直到收益趋于平稳。"),
        ("在 1980 年代末，非洲越来越的批评家认同这一观点", "在 1980 年代末，非洲越来越多的批评家认同这一观点"),
        (
            "在没有欧洲人定居的国家，取代殖民统治的本土政府，其领导人和行政人员，都来自于没有任何管理大型组织经验或技能的人口。在很多情况下，特别是在非洲，殖民国家离开以后，当地继承下来的基础设施，很快就遭到抢夺、破坏，或者任其失修。电话线被拾荒者拆毁，并砸成手镯。道路得不到维护。在没有被欧洲人定居的国家，取代殖民统治的土著政府，其领导人和行政人员都来自于没有什么经验或技能来管理任何类型的大型企业的人口。由于路基崩塌和机车故障，铁路线变得毫无用处。在扎伊尔（非洲中西部国家），比利时人精心设计和建造的交通基础设施，到 1990 年几乎全部消失。只有几艘吱吱作响的河船还在继续飘荡，其中一艘被独裁者接管，改成了浮动的宫殿。",
            "在没有欧洲人定居的国家，取代殖民统治的本土政府，其领导人和行政人员大多来自缺乏大型组织管理经验和技能的人群。在很多情况下，特别是在非洲，殖民国家离开以后，当地继承下来的基础设施很快就遭到抢夺、破坏，或者任其失修。电话线被拾荒者拆毁，砸成手镯；道路无人维护；由于路基崩塌和机车故障，铁路线变得毫无用处。在扎伊尔（非洲中西部国家），比利时人精心设计和建造的交通基础设施，到 1990 年几乎全部消失。只有几艘吱吱作响的河船还在继续飘荡，其中一艘被独裁者接管，改成了浮动的宫殿。",
        ),
        ("这些变化都有利于落后国家的人民，可以减轻他们生活在无能政府下所遭受的伤害。", "这些变化都有利于落后国家的人民，可以减轻他们因生活在无能政府治下而长期承受的代价。"),
        ("各国政府不得不制定政策，去吸引能对经济发展做出最大贡献的客户，而不是懒汉和寄生虫。", "各国政府不得不制定政策，去吸引能对经济发展做出最大贡献的客户，而不是那些主要依赖补贴、却难以创造价值的人。"),
        ("施加了过高成本的地方性法规，将会转变得更加立足于市场。", "那些施加过高成本的地方性法规，将被迫转向更市场化的基础。"),
        ("大宗商品会像中世纪那样，被课以重税，并在当地内运输；而奢侈品的税收则很轻，并可以运输到很远的地方。", "大宗商品会像中世纪那样被课以重税，并主要在本地范围内运输；而奢侈品的税收则很轻，可以运到很远的地方。"),
        ("私营公司比上市公司更具转型优势", "在过渡阶段，私营公司相较上市公司可能更具优势"),
        ("因为在产品种注入知识，会使创造财富越来越容易。", "因为向产品中注入知识，会使创造财富越来越容易。"),
        ("暴力组织中的经济不规模现象", "暴力组织中的规模不经济现象"),
        ("确实由客户控制地方主权的地区数量，应该会迅速增加。", "那些确实由客户控制地方主权的地区，数量应会迅速增加。"),
        ("接下来我们将会探讨，面对失败团体的反对这些变革与发展能否或是否应该进行下去。", "接下来我们将会探讨，面对大批失利者的反对，这些变革究竟能否、或是否应该继续推进。"),
        ("掌握了创业、投资等特殊技能的大师", "掌握创业、投资等特殊技能的高端人才"),
        ("会面临都是障碍", "会面临重重障碍"),
        ("几十年的最大的企业", "经营数十年的大型企业"),
        ("事所必然", "这几乎是必然的"),
        ("他们将有权自己的保留地和自然保护区进行税收管辖，就像现在有权经营赌场，或不受限制地捕鱼一样。", "他们将有权在自己的保留地和自然保护区内行使税收管辖，就像现在有权经营赌场或不受限制地捕鱼一样。"),
    ],
    "11-chapter9.md": [
        ("“智能炸弹客”", "“独行炸弹客”"),
        ("特急葡萄酒", "顶级波尔多红酒"),
        ("苯尼迪克特·安德森", "本尼迪克特·安德森"),
        ("官僚智慧系统", "官僚指挥链"),
        (
            "最值得注意的是，印刷术对异端思想的传播起了决定性的推进作用。我们期待互联网对个人的非国有化，也会产生同样的推进力。",
            "最值得注意的是，印刷术对异端思想的传播起了决定性的推进作用。我们期待互联网对个人去国家化的进程，也会产生同样的推进力。",
        ),
        ("对粉丝的利他主义", "对亲属的利他主义"),
        ("需求利益最大化的基本单位", "寻求利益最大化的基本单位"),
        ("顽固不化的亲属利他主义", "化石化的亲属利他主义"),
        ("“被遗弃者”（left-hebinds）", "“被甩在后面的人”（left-behinds）"),
        ("技能地下", "技能低下"),
        (" center 救世军将军内德·卢德 签名", " —— 救世军将军内德·卢德 签名"),
        ("书记员 救世军万岁 阿门 center", "书记员。救世军万岁。阿门。"),
        ("挥舞重大 50 磅的巨大剪刀", "挥舞重达 50 磅的巨大剪刀"),
        ("主权国家之间的状态一直是无政府主义的；", "主权国家彼此之间一直处于无政府状态；"),
        ("“两个或多个无政府主义竞争者”", "“两个或多个处于无政府状态的竞争者”"),
        ("像美国这样的猪头", "像美国这样顽固的"),
        ("在20世纪无所不在", "在 20 世纪无所不在"),
        ("1000年时的革命过程中", "1000 年时的革命过程中"),
        ("在互联网的意义上，整个世界是一个非常小的社区", "从互联网的尺度看，整个世界会变成一个极小的社区"),
        (
            "他认为，在一定条件下“两个或多个无政府主义竞争者”，可以“在平衡的状态下，从社会可用资源中分获供各自生存的份额。”",
            "他认为，在一定条件下，两个或多个处于无政府状态下的竞争者，可以“在平衡的状态下，从社会可用资源中分获供各自生存的份额”。",
        ),
        (
            "即使像美国这样的猪头[^11-chapter9-fn-29]强国，在当前的趋势下，也必须大力防止出现不受政府控制的网络经济；",
            "即使像美国这样顽固的[^11-chapter9-fn-29]大国，在当前趋势下，也必须竭力防止出现不受政府控制的网络经济；",
        ),
    ],
    "12-chapter10.md": [
        (
            "例如在 1996 年，美国最高等级的联邦终身税率为 1 美元 73 美分。对于通过分红获得收入的公司股东来说，税率为 1 美元 83 美分。而对于任何想把财产留给孙子女的人来说，联邦税率为 1 美元 93 美分。",
            "例如在 1996 年，美国最高等级的联邦终身税率，相当于每赚 1 美元就要缴出 73 美分。对于通过分红获得收入的公司股东来说，税率相当于每美元缴出 83 美分。而对于任何想把财产留给孙子女的人来说，联邦税率则相当于每美元缴出 93 美分。",
        ),
        ("外国科学家在美国工作", "外国数学家在美国工作"),
        ("### 电子投票", "### 电子公决"),
        ("电子投票。全体公民", "电子公决。全体公民"),
        ("50 平米公里", "50 平方公里"),
        ("决定哪些人可以进补候选的职位", "决定哪些人可以进入候选职位的抽签范围"),
        ("“中层选民”", "“中位选民”"),
        ("免幸免于是", "幸免于此"),
        ("在这种情况下，为一个管辖区制定政策，在选择它的可实行范围时，就必须适当地缩小；", "在这种情况下，一个管辖区在组装政策方案时，可行的选择范围会被有效收窄；"),
        ("那些权力被分解地区的民众", "那些处于权力分解过程中的管辖区居民"),
        ("直接扣押富人的钱", "把富人当成勒索对象，近乎公开索取赎金"),
        ("否则的话，通过集体决策去强制推行不经济的结果有什么意义呢？", "否则，靠集体决策去强行制造违背市场结果的安排，也就失去了意义。"),
        ("企业甚至不会以人们所熟悉的现代形式继续存在；这一点并不显为人知。一点也不。", "事实上，企业是否还会以人们熟悉的现代形式继续存在，都远非显而易见。"),
        ("其实就像我们所说的，事实上，企业是否还会以人们熟悉的现代形式继续存在，都远非显而易见。", "正如我们前面所说，企业是否还会以人们熟悉的现代形式继续存在，都远非显而易见。"),
        ("通过信息技术依靠价格机制和市场竞标来完全相应的工作任务", "通过信息技术依靠价格机制和竞价市场来完成相应的工作任务"),
        ("并不像偶尔闪过的那么微不足道。", "并不像乍看之下那样微不足道。"),
        ("在政府和第三部门间之间", "在政府和第三部门之间"),
        ("为什么会有公司这个问题，并不像偶尔闪过的那么微不足道。", "为什么会有公司这个问题，并不像乍看之下那样微不足道。"),
        ("“在政府和第三部门间之间，构建新的伙伴关系，以重建社会经济。", "“在政府和第三部门之间，构建新的伙伴关系，以重建社会经济。"),
        ("作为一个个人，如果你在经济上实现了足够的独立，那么你就会像阿吉雷一样，获得终极的独立。", "作为个人，如果你在经济上实现了足够程度的独立，那么你就会像阿吉雷一样，获得终极的独立。"),
        ("所以，在未来你不用再与煽动家和政治掮客进行拉锯战，防止你的资产被大众民主的叫嚣者夺走并瓜分；你可以建立自己的私人领地，进行私人治理。", "因此，未来你不必再与煽动家和政治掮客反复周旋，防止自己的资产被大众民主的鼓噪者夺走并瓜分；你可以建立自己的私人领地，实行私人治理。"),
    ],
    "13-chapter11.md": [
        ("代表着过去的政党。有些人则被不合适应的社会主义", "代表着过去。有些人则被不适应时代的社会主义"),
        ("就是是世界上的超级军事强国", "即使是世界上的超级军事强国"),
        ("因为信息技术削弱了，政府实施权力以及为大规模组织提供物理安全的能力；而且，“自然经济”中每家企业的最优规模都在下降。", "因为信息技术削弱了政府投射权力以及为大规模组织提供物理安全的能力；而且，在“自然经济”中，每家企业的最优规模都在下降。"),
        ("产品于服务", "产品与服务"),
        ("缉毒队对长", "缉毒队队长"),
        ("买通一个地方的警察办公室", "买通一个地方警察职位"),
        ("连希拉里都不知道", "连希拉里都不会说"),
        ("中情局与总统合署办公", "中情局已经把总统职位收编了"),
        ("一章文章", "一篇文章"),
        ("哈米什·麦克雷（Hamish McRae）大师", "哈米什·麦克雷（Hamish McRae）"),
        ("> center 不要说奋斗终是徒劳，辛劳和创伤白费无功，", "> 不要说奋斗终是徒劳，辛劳和创伤白费无功，"),
        ("> 但向西看吧，遍地洒满霞光！ center", "> 但向西看吧，遍地洒满霞光！"),
        (
            "副标题为：“自然选择下的熊——生存斗争的广义用法——按几何比率的增长——归化的动物和植物的迅速增长——抑制增长的性质——斗争的普遍性——气候的影响——个体数量的保护——一切动植物在自然界中的复杂关系——同物种的个体间和变体间的斗争最为剧烈，同属的物种间的斗争也很剧烈——有机体与有机体间的关系是一切关系中最重要的”",
            "副标题为：“生存斗争的广义用法——按几何比率的增长——归化的动物和植物的迅速增长——抑制增长的性质——斗争的普遍性——气候的影响——个体数量的保护——一切动植物在自然界中的复杂关系——同物种的个体间和变体间的斗争最为剧烈，同属的物种间的斗争也很激烈——有机体与有机体间的关系是一切关系中最重要的”",
        ),
        ("道德率", "道德律"),
        ("罗马帝国后期", "罗马共和国晚期"),
        ("原来越疏远", "越来越疏远"),
        ("深深的用户", "深切持守"),
        ("排斥主义", "排斥机制"),
        ("欺骗着", "欺骗者"),
        ("会变得越来越炽盛，不然那很难从个人手里夺走资源", "会变得越来越猖獗；否则就很难从个人手里夺走资源"),
        ("私会", "私下会面"),
        ("继兄弟姐妹见的道德观念各有不同。", "继兄弟姐妹间的道德观念各有不同。"),
        ("从商业和共情中，产生的相互依存的逻辑", "商业与同理之情所生成的相互依存逻辑"),
        ("这个故事真的是一波三折，扭曲离奇。", "整个故事可谓曲折离奇，几近荒诞。"),
        ("一个更好的罪犯阶层", "一个层级更高的罪犯群体"),
        ("“Caveat emptor”即“买家自己小心”，对他们来说是不够好的。", "对他们而言，仅仅遵循“买者自慎”（caveat emptor）的原则还远远不够。"),
        ("在 1995年", "在 1995 年"),
        ("到2005 年，私人安保的数量，将比 1990 年增加 24%到 40%。", "到 2005 年，私人安保人员的数量预计将比 1990 年增加 24% 到 40%。"),
        ("辐射性灾难令人望而生畏", "其可能引发的连锁性后果令人望而生畏"),
        ("负结果反馈机制", "负反馈机制"),
        ("反作用力文化", "适得其反的文化"),
        ("它应该得到广泛的认同和深深的用户。", "它应该得到广泛认同，并被人们深切持守。"),
        ("如果加密方式或加密身份的验证被不法之徒或其他人所破坏，它可能导致的辐射性灾难令人望而生畏；", "如果加密方式或加密身份的验证被不法之徒或其他人所破坏，其可能引发的连锁性后果将令人望而生畏；"),
        ("如果加密方式或加密身份的验证被不法之徒或其他人所破坏，它可能导致的其可能引发的连锁性后果令人望而生畏；", "如果加密方式或加密身份的验证被不法之徒或其他人所破坏，其可能引发的连锁性后果将令人望而生畏；"),
        ("如果加密方式或加密身份的验证被不法之徒或其他人所破坏，它可能导致的其可能引发的连锁性后果令人望而生畏；这一点就足以使人们强烈排斥雇佣任何缺乏可信度的人。", "如果加密方式或加密身份的验证被不法之徒或其他人所破坏，其可能引发的连锁性后果将令人望而生畏；这一点就足以使人们强烈排斥雇用任何缺乏可信度的人。"),
        ("在互联网的意义上，整个世界是一个非常小的社区，骗子和小偷将面临更大的压力。", "从互联网的尺度看，整个世界会变成一个极小的社区，骗子和小偷将承受更大的压力。"),
        ("而对于认识反作用力文化的弊端，则非常迟钝。", "而对于识别那些适得其反的文化弊端，却显得异常迟钝。"),
    ],
    "14-afterword.md": [
        ("罗伯特·格林 朱斯特·艾尔弗斯", "罗伯特·格林、朱斯特·艾尔弗斯"),
        ("考古学家和历史学建约瑟夫·坦特", "考古学家兼历史学家约瑟夫·坦特"),
        ("使之朝着更高水平的集中化发展", "使这一明显“不可阻挡的趋势”转而背离更高程度的集中化"),
        ("例如美国的税收总额从 1957 年的占到收入中位数的 27.8%，上升到 1997 年的 37.6%；", "例如，美国税负总额占收入中位数的比例，从 1957 年的 27.8% 上升到 1997 年的 37.6%；"),
        ("10%的美国人缴纳了全国大部分的所得税", "0.1% 的美国人缴纳了全国大部分所得税"),
        ("没有愿意加班并缴纳没收性税收的好人意外地涌入", "没有愿意加班并承受没收性税率的“天使”意外涌入"),
        ("研究和分析各种大师的作品", "研究和评估各种成功学作者的作品"),
        ("你无需再缴纳贪婪政客所强加的高额税负", "你无需再承担民族国家强加的高额税负"),
        ("将不会超过它们对你所具备的实际价值。", "将不会超过这些服务对你的实际价值。"),
        ("你优先要做的一件事，就是研究和评估各种成功学作者的作品", "你首先要做的一件事，就是广泛研究那些讨论成功与财富积累的作者及其作品"),
        ("所以，你不会因为学了一些用阿拉丁神灯也能完成的技能而获得高薪。", "因此，你不会仅仅因为掌握了一项日后可能被自动化轻易替代的技能，就长期获得高薪。"),
        (
            "然而，在当今这个时代，人类经济发展的下一阶段——信息社会的出现，有望扭转这种明显的“不可阻挡的趋势”，使这一明显“不可阻挡的趋势”转而背离更高程度的集中化。",
            "然而，在当今这个时代，人类经济发展的下一阶段——信息社会的出现，有望扭转这种看似“不可阻挡的趋势”，使社会的发展方向背离更高程度的集中化。",
        ),
        ("根据历史上统治系统崩溃前的情况来看，那些提前选好了最后的避难所并尽早逃离的人，会因此而生活得更好。", "回顾历史上统治体系崩溃前夕的情形，那些提前选定最后避难所并尽早离开的人，往往最终过得更好。"),
        ("这点现在已经很明显了，从在 20 世纪 90 年代，美国通过了大量的法律，惩罚那些放弃美国公民身份的人。", "这一点如今已十分明显：早在 20 世纪 90 年代，美国就通过了大量法律，惩罚那些放弃美国公民身份的人。"),
        ("这一点现在已经很明显了，从在 20 世纪 90 年代，美国通过了大量的法律，惩罚那些放弃美国公民身份的人。", "这一点如今已十分明显：早在 20 世纪 90 年代，美国就通过了大量法律，惩罚那些放弃美国公民身份的人。"),
        ("美国可能会是阻击商业化主权的最暴虐的国家之一。", "美国可能会是阻击商业化主权最为暴虐的国家之一。"),
        ("你永远不能把自己的财产，留在任何声称有权力征收你和你子孙的管辖区内。", "你永远不能把自己的财产留在任何自称有权向你及子孙征敛的管辖区内。"),
        ("为了优化财富，你应该主要居住在第一护照国以外的国家，同时把你的大部分资金存放在第三管辖区，最好是避税天堂。", "为了更妥善地配置财富，你应主要居住在第一本护照所属国以外的国家，同时把大部分资金存放在第三管辖区，最好是税负较轻的离岸金融中心。"),
        ("此外，为了更好地了解更多选择，我们建议你广泛地旅行，访问一些你觉得很有吸引力的地方，然后确保在紧急情况下你有权过去居住。", "此外，为了更充分地了解可选方案，我们建议你广泛旅行，考察那些对你具有吸引力的地方，并确保在紧急情况下你拥有前往当地居住的合法资格。"),
    ],
    "16-appendix2.md": [
        ("# 附录2：实现财务独立的资源推荐", "# 附录二：实现财务独立的资源推荐"),
        ("这是由詹姆斯·戴尔·戴维森和里斯·莫格勋爵提供的私人财务顾问服务。", "这是一本由詹姆斯·戴尔·戴维森和里斯·莫格勋爵主编的私人财务咨询刊物。"),
        ("私人财务咨询刊物", "私人理财通讯刊物"),
        ("精准推出了一个鲜为人知的苏联政治局成员", "准确点出了当时鲜为人知的苏联政治局成员"),
        ("电话；(703) 836-8250。", "电话：(703) 836-8250。"),
        ("为商业机头融资", "为商业项目融资"),
        ("有关替换护照的信息", "有关替代性护照的信息"),
        ("詹姆斯·戴尔·戴维森和里斯·莫格勋爵主编", "詹姆斯·戴尔·戴维森和里斯·莫格勋爵主持编辑"),
        ("地址： 108 N. Alfred Street, Alexandria, Virginia, Suite 200 22314。电话（703）548-836-8250 或 888-281-8250。", "地址：108 N. Alfred Street, Suite 200, Alexandria, Virginia 22314。电话：(703) 836-8250 或 888-281-8250。"),
        ("他们联合起来，帮助彼此以实现独立。", "他们联合起来，彼此扶持，以实现独立。"),
        ("该团体创作和传播有关替代性护照的信息", "该团体整理并传播有关替代性护照的信息"),
        ("欲了解更多信息，请联系主权协会，地址是 105 W. Monument Street, Baltimore, Maryland 21202。或访问他们的网站：http://www.sovereignsociety.com。", "欲了解更多信息，请联系主权协会，地址：105 W. Monument Street, Baltimore, Maryland 21202；或访问其网站：http://www.sovereignsociety.com。"),
        ("如果你喜欢本书，你可能也会喜欢阅读《战略投资》", "如果你喜欢本书，你或许也会想读《战略投资》"),
        ("试订《战略投资》6 个月", "试订《战略投资》六个月"),
        ("Alexandria, VA22314", "Alexandria, VA 22314"),
        ("1990年日本股市崩盘、1991 年海湾战争中伊拉克的溃败、以及苏联的解体。", "1990 年日本股市崩盘、1991 年海湾战争中伊拉克的溃败，以及苏联解体。"),
        ("《战略投资》为读者提供了关于信息经济崛起的持续洞察力，以及如何从地缘政治发展中获益的建议。", "《战略投资》持续追踪信息经济的崛起，并为读者提供如何从地缘政治变化中获益的建议。"),
        ("比推土机实际推倒墙体的时间早了十个月", "比推土机真正推倒柏林墙早了十个月"),
        ("《战略投资》就已经告诉了读者该如何应对。", "《战略投资》就已经提醒读者该如何应对。"),
        ("另外，请登录战略投资网站浏览 http://www.strategicinvestment.com。", "另外，请访问《战略投资》网站：http://www.strategicinvestment.com。"),
        ("普通会员资格是每年 295美元。", "普通会员资格为每年 295 美元。"),
        ("如果你想与詹姆斯·戴尔·戴维森和里斯·莫格勋爵一起，参加新兴机遇之地的替代性住所和生活方式之旅，请联系丽莎·伊登，电话：888-281-8250。", "如果你想与詹姆斯·戴尔·戴维森和里斯·莫格勋爵一起，前往新兴机会之地考察替代性居所与生活方式，请联系丽莎·伊登，电话：888-281-8250。"),
    ],
    "15-appendix.md": [
        ("# 附录1：影响和战略", "# 附录一：影响和战略"),
        ("为了最大化你的终身收益，成为主权个人，你应该成为政府或其保护服务的客户，而不再是公民。", "为了使你的终身收益最大化，并成长为主权个人，你应当成为政府或其保护服务的客户，而不再只是公民。"),
        ("你无需再缴纳贪婪政客所强加的高额税负", "你无需再承担民族国家强加的高额税负"),
        ("作为实现私有化或去国家化的必要步骤。", "作为迈向去国家化的必要步骤。"),
        ("选择最后的庇护所，并尽早退出迁移过去的人会笑到最后。", "选好最后的避难所，并及早迁离旧体系的人，会笑到最后。"),
        ("为了优化你的财务管理，你应该主要居住在你第一护照国以外的国家，同时将大部分资产保存在第三国，最好是避税天堂。", "为了更妥善地配置你的财富，你应主要居住在第一本护照所属国以外的国家，同时把大部分资产存放在第三管辖区，最好是税负较轻的离岸金融中心。"),
        ("您应该广泛旅行，在有吸引力的地方选择替代性居住地；当紧急情况发生时，你要有权利自由进出那里。", "你应广泛旅行，在有吸引力的地方选定替代性居住地；一旦紧急情况发生，你要有权自由进出那里。"),
        ("在工业时代富得流油的经济体", "在工业时代极为富裕的经济体"),
        ("沦为那些大胆的投资者的犯罪场所；", "沦为胆大投资者眼中的犯罪性淘金地带；"),
        ("下个世纪（21 世纪），增长最快的、最重要的新经济将不是中国，而是网络经济。", "在 21 世纪，增长最快、也最重要的新经济将不是中国，而是网络经济。"),
        ("美国和其他有控制力的政府", "美国和其他强势政府"),
        ("在可能的情况下，所有的企业都应该在避税管辖区进行离岸注册。", "在条件允许的情况下，企业都应考虑在低税负管辖区进行离岸注册。"),
        ("信息时代的公司，将越来越多地成为 \"虚拟公司\"，靠契约关系约束，没有现实的实体，可能也没有物质资产。", "信息时代的公司将越来越多地成为“虚拟公司”，它们靠契约关系维系，没有现实实体，也可能没有物质资产。"),
        ("对资源的控制权，将会从官方转移到那些智慧和能力超群的人（如马斯克）手上，因为在产品中注入知识将会创造更多的财富。", "对资源的控制权，将会从官方转移到那些智慧和能力格外出众的人手中，因为在产品中注入知识会创造更多财富。"),
        ("以前在体制内找工作的风险偏好者，可能会倒向另一种选择，就是服侍有钱人。", "那些过去偏好体制内工作的风险承担者，可能会转向另一条路：为富人效力。"),
        ("降低升本，以灵活的条款去制定长期的和合同和酬劳方案。", "尽快节省开支、降低成本，并以更灵活的条款安排长期合同与薪酬方案。"),
        ("50% 70%", "50% 至 70%"),
        ("那些不具有创造产品或服务的传统的国家，不太可能会引进代替就业的技术创新。", "那些原本就缺乏创造产品或服务传统的国家，不太可能会引进替代就业的技术创新。"),
        ("将不会超过它们对你所具备的实际价值。", "不会超过这些服务对你的实际价值。"),
        ("拿美国身份，在经济上是很不理性的；", "寻求美国身份，在经济上并不理性；"),
        ("那些能够根据其他统治体系面临崩溃的历史，选好最后的避难所", "那些能够从其他统治体系崩溃的历史中汲取教训、选好最后避难所的人"),
        ("在工业时代极为富裕的经济体", "在那些工业时代极为富裕的经济体中"),
        ("48个最不发达的国家", "48 个最不发达的国家"),
        ("沦为胆大投资者眼中的犯罪性淘金地带；", "沦为胆大投资者眼中的高风险淘金地带；"),
        ("沦为胆大投资者眼中的高风险淘金地带；", "沦为高风险偏好投资者眼中的淘金地带；"),
        ("网络货币将成为信息时代的新货币", "网络货币将成为信息时代的主要货币形态之一"),
        ("去适应通缩的陌生现实，也就是说，应该避免负债，并迫切地追求节约和尽快节省开支、降低成本，并以更灵活的条款安排长期合同与薪酬方案。", "去适应通缩这一陌生现实；也就是说，应当避免负债，并尽快节流、降低成本，以更灵活的条款安排长期合同与薪酬方案。"),
    ],
}

FOOTNOTE_ACTIONS: dict[str, dict[str, dict[str, str]]] = {
    "02-preface2.md": {
        "02-preface2-fn-1": {"action": "remove", "reason": "宣传性个人网站链接，不纳入主书脚注。"},
    },
    "03-chapter1.md": {
        "03-chapter1-fn-2": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长生平细节。",
            "replacement": "译注：埃斯库罗斯（Aeschylus）是古希腊悲剧诗人，常被称为“悲剧之父”。",
        },
        "03-chapter1-fn-4": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长家世与财富细节。",
            "replacement": "译注：乌萨马·本·拉登（Osama bin Laden）是“基地”组织创始人，后被普遍认定为“9·11”袭击主谋。",
        },
        "03-chapter1-fn-5": {
            "action": "rewrite",
            "reason": "压缩为必要概念说明，移除冗长的现代延伸。",
            "replacement": "译注：“卢德分子”原指 19 世纪初英国反对机械化生产的工人；“新卢德主义”则借此概念批评技术扩张对社会与劳动的冲击。",
        },
        "03-chapter1-fn-7": {
            "action": "rewrite",
            "reason": "压缩为必要组织说明，移除冗长历史背景。",
            "replacement": "译注：马耳他骑士团是历史悠久的天主教骑士团体，今天仍以准主权实体身份存在于国际社会。",
        },
        "03-chapter1-fn-9": {
            "action": "rewrite",
            "reason": "压缩为必要地名说明，移除冗长建筑史细节。",
            "replacement": "译注：圣安杰洛城堡即罗马的天使城堡，长期兼具堡垒与教廷防御设施功能。",
        },
        "03-chapter1-fn-10": {
            "action": "rewrite",
            "reason": "将现代类比式译注压缩为中性说明。",
            "replacement": "译注：铱星计划是 1990 年代的全球卫星电话网络构想。",
        },
        "03-chapter1-fn-11": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长履历。",
            "replacement": "译注：尼尔·斯蒂芬森（Neal Stephenson）是美国科幻作家，以《雪崩》提出“元宇宙”概念而闻名。",
        },
        "03-chapter1-fn-12": {
            "action": "rewrite",
            "reason": "压缩为必要历史说明，移除冗长组织史细节。",
            "replacement": "译注：汉萨同盟是中世纪后期德意志北部诸城市形成的商业联盟。",
        },
        "03-chapter1-fn-13": {
            "action": "rewrite",
            "reason": "压缩为必要地名与事件说明，移除冗长背景。",
            "replacement": "译注：这里借布加勒斯特指代 1989 年罗马尼亚政权崩溃与东欧剧变中的暴力转折。",
        },
        "03-chapter1-fn-14": {
            "action": "rewrite",
            "reason": "压缩为必要事件说明，移除过长细节。",
            "replacement": "译注：这里指 1995 年的俄克拉何马城爆炸案；在“9·11”之前，它被普遍视为美国本土最严重的恐怖袭击之一。",
        },
        "03-chapter1-fn-15": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长履历。",
            "replacement": "译注：威廉·普莱费尔（William Playfair）是苏格兰工程师与经济学家，也是统计图表的早期推广者。",
        },
        "03-chapter1-fn-16": {
            "action": "rewrite",
            "reason": "移除外链，压缩为必要作品说明。",
            "replacement": "译注：这里指一部探讨国家由盛转衰机制的 19 世纪历史研究著作。",
        },
        "03-chapter1-fn-17": {
            "action": "rewrite",
            "reason": "移除裸链接，保留中性说明。",
            "replacement": "译注：此处所引论文可在 JSTOR 检索到原文。",
        },
        "03-chapter1-fn-18": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长生平细节。",
            "replacement": "译注：莫妮卡·莱温斯基（Monica Lewinsky）曾因与克林顿有关的政治丑闻而广为人知。",
        },
        "03-chapter1-fn-19": {
            "action": "rewrite",
            "reason": "压缩为必要作品说明。",
            "replacement": "译注：The Skin of Our Teeth 是一部以人类文明反复受创又再度延续为主题的戏剧。",
        },
        "03-chapter1-fn-20": {
            "action": "rewrite",
            "reason": "压缩为必要术语说明，移除教科书式背景介绍。",
            "replacement": "译注：COBOL 是一种长期用于商业数据处理的编程语言。",
        },
        "03-chapter1-fn-22": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长生平细节。",
            "replacement": "译注：菲奥雷的约阿希姆（Joachim of Fiore）是中世纪末以末世论和历史分期思想著称的宗教思想家。",
        },
        "03-chapter1-fn-26": {
            "action": "rewrite",
            "reason": "压缩为必要术语说明。",
            "replacement": "译注：strange attractor，通常译作“奇异吸引子”，是非线性动力学中的概念。",
        },
    },
    "04-chapter2.md": {
        "04-chapter2-fn-1": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除履历细节。",
            "replacement": "译注：沃尔夫·布利策（Wolf Blitzer）是美国资深电视新闻主持人。",
        },
        "04-chapter2-fn-2": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明。",
            "replacement": "译注：威尔·杜兰特（Will Durant）是美国历史学者，以《世界文明史》闻名。",
        },
        "04-chapter2-fn-3": {
            "action": "rewrite",
            "reason": "移除外链，保留中性参考说明。",
            "replacement": "译注：此处原注所引为相关中文参考书目。",
        },
        "04-chapter2-fn-4": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明。",
            "replacement": "译注：C. W. Previte-Orton 是英国中世纪史学者。",
        },
        "04-chapter2-fn-5": {
            "action": "rewrite",
            "reason": "压缩为必要作品说明，移除评价性延伸。",
            "replacement": "译注：这里指一位耶鲁历史学家及其 1988 年出版的《腐败与罗马的衰亡》。",
        },
        "04-chapter2-fn-6": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长生平细节。",
            "replacement": "译注：这里指西奈西乌斯（Synesius），晚期古典世界的主教、哲学家和作家。",
        },
    },
    "05-chapter3.md": {
        "05-chapter3-fn-5": {
            "action": "rewrite",
            "reason": "压缩为必要术语说明，移除冗长词源与地域细节。",
            "replacement": "译注：Kaffer 或 Kafir 原意为“异教徒”，后来在某些殖民语境中演变为带有侮辱性的称呼。",
        },
        "05-chapter3-fn-7": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长学术履历。",
            "replacement": "译注：拉德克利夫-布朗（Alfred Radcliffe-Brown）是英国社会人类学的重要代表人物之一。",
        },
    },
    "06-chapter4.md": {
        "06-chapter4-fn-1": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长履历。",
            "replacement": "译注：马丁·范·克雷维尔德（Martin van Creveld）是以战争史与军事理论研究闻名的历史学家。",
        },
        "06-chapter4-fn-2": {
            "action": "rewrite",
            "reason": "压缩为必要作品说明，移除教科书式背景。",
            "replacement": "译注：《辩谬篇》（On Sophistical Refutations）是亚里士多德逻辑学著作之一。",
        },
        "06-chapter4-fn-3": {
            "action": "rewrite",
            "reason": "压缩为必要历史说明。",
            "replacement": "译注：这里指 1517 年马丁·路德公开批评赎罪券、引发宗教改革的著名事件。",
        },
        "06-chapter4-fn-4": {"action": "remove", "reason": "现代稳定币立法时评，与原书边界不符。"},
        "06-chapter4-fn-5": {
            "action": "rewrite",
            "reason": "压缩为必要地名说明，移除冗长建筑史细节。",
            "replacement": "译注：这里指位于意大利卡塔尼亚的本笃会修道院，现为当地重要历史建筑之一。",
        },
        "06-chapter4-fn-6": {
            "action": "rewrite",
            "reason": "压缩为必要术语说明，移除冗长制度史细节。",
            "replacement": "译注：什一税（tithe）是欧洲基督教社会长期存在的一种宗教捐税。",
        },
        "06-chapter4-fn-7": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长生平细节。",
            "replacement": "译注：马基雅维利（Niccolo Machiavelli）是意大利政治思想家，以《君主论》闻名。",
        },
        "06-chapter4-fn-8": {
            "action": "rewrite",
            "reason": "压缩为必要情节说明。",
            "replacement": "译注：这里借一则带有讽刺意味的教会逸闻，说明修道纪律的败坏。",
        },
    },
    "07-chapter5.md": {
        "07-chapter5-fn-1": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除近年时评和无关近况。",
            "replacement": "译注：弗朗西斯·福山（Francis Fukuyama），日裔美籍政治学者，以《历史的终结？》及相关论述闻名。",
        },
        "07-chapter5-fn-4": {
            "action": "rewrite",
            "reason": "统一人名并压缩为必要人物说明，移除近年案件细节。",
            "replacement": "译注：阿尔韦托·藤森（Alberto Fujimori），1990 至 2000 年任秘鲁总统；其激进改革与强人统治在秘鲁社会一直评价不一。",
        },
        "07-chapter5-fn-5": {
            "action": "rewrite",
            "reason": "压缩为必要作品说明，移除剧情和奖项细节。",
            "replacement": "译注：《发条橙》（A Clockwork Orange）是安东尼·伯吉斯的小说，也是斯坦利·库布里克的电影改编作品，以暴力、秩序与社会控制主题闻名。",
        },
        "07-chapter5-fn-6": {
            "action": "rewrite",
            "reason": "压缩为必要组织说明，移除过长背景延伸。",
            "replacement": "译注：光辉道路（Sendero Luminoso）是秘鲁极左武装组织，主张以暴力革命夺取政权，1980 至 1990 年代曾在秘鲁多地发动严重袭击。",
        },
    },
    "08-chapter6.md": {
        "08-chapter6-fn-1": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除近年观点延伸。",
            "replacement": "译注：凯文·凯利（Kevin Kelly），《连线》杂志创始主编之一，长期关注技术与网络文化。",
        },
        "08-chapter6-fn-3": {
            "action": "rewrite",
            "reason": "压缩为必要机构说明，移除企业史细节。",
            "replacement": "译注：Chiat/Day 是以创意广告著称的美国广告公司，苹果经典广告《致疯狂的人》即出自其手。",
        },
        "08-chapter6-fn-4": {
            "action": "rewrite",
            "reason": "压缩为必要作品说明，移除延伸阐释。",
            "replacement": "译注：大卫·埃尔金斯在《超越主权》中讨论了信息时代主权与领土可能分离的趋势。",
        },
        "08-chapter6-fn-7": {
            "action": "rewrite",
            "reason": "删除与原书无关的近年行政信息。",
            "replacement": "译注：安道尔是位于比利牛斯山脉的袖珍主权国家，由法国与西班牙共同承认其防务安排。",
        },
    },
    "09-chapter7.md": {
        "09-chapter7-fn-2": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除评价性延伸。",
            "replacement": "译注：艾伦·格林斯潘（Alan Greenspan）曾长期担任美国联邦储备委员会主席。",
        },
        "09-chapter7-fn-3": {"action": "remove", "reason": "现代稳定币补充说明，不纳入主书脚注。"},
    },
    "10-chapter8.md": {
        "10-chapter8-fn-2": {
            "action": "rewrite",
            "reason": "压缩为必要城市背景说明，移除冗长统计细节。",
            "replacement": "译注：南本德（South Bend）是美国印第安纳州北部城市，曾是典型工业城市，后长期经历人口流失与产业转型。",
        },
        "10-chapter8-fn-3": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长履历。",
            "replacement": "译注：罗纳德·科斯（Ronald Coase）是新制度经济学的重要代表人物，以“科斯定理”著称。",
        },
        "10-chapter8-fn-4": {
            "action": "rewrite",
            "reason": "压缩为必要概念说明，移除冗长教科书式定义。",
            "replacement": "译注：卡特尔（cartel）指企业通过协定限制竞争、操纵价格或产量的垄断联盟。",
        },
    },
    "11-chapter9.md": {
        "11-chapter9-fn-1": {
            "action": "rewrite",
            "reason": "移除裸链接，保留中性来源说明。",
            "replacement": "译注：此处原注引 IBM 相关资料。",
        },
        "11-chapter9-fn-14": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长生平细节。",
            "replacement": "译注：约翰·古腾堡（Johannes Gutenberg）是西方活字印刷术的关键推动者。",
        },
        "11-chapter9-fn-17": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除评价性延伸。",
            "replacement": "译注：克里斯托弗·拉什（Christopher Lasch）是美国历史学家与社会批评家，著有《自恋主义文化》等书。",
        },
        "11-chapter9-fn-18": {
            "action": "rewrite",
            "reason": "移除外链，保留必要书目信息。",
            "replacement": "译注：此书原名为《The Revolt of the Elites and the Betrayal of Democracy》。",
        },
        "11-chapter9-fn-20": {"action": "remove", "reason": "2025 年 RWA 行业补注，非原书内容。"},
        "11-chapter9-fn-21": {"action": "remove", "reason": "按现值换算的现代补注，时效性过强。"},
        "11-chapter9-fn-22": {"action": "remove", "reason": "宣传性个人网站链接，不纳入主书脚注。"},
        "11-chapter9-fn-23": {"action": "remove", "reason": "2022 年政治新闻脚注，与原书边界不符。"},
        "11-chapter9-fn-24": {
            "action": "rewrite",
            "reason": "压缩为必要人物说明，移除冗长政治履历。",
            "replacement": "译注：久加诺夫（Gennady Zyuganov）是俄罗斯共产党主要领导人之一。",
        },
        "11-chapter9-fn-27": {
            "action": "rewrite",
            "reason": "压缩为必要历史说明，移除教科书式概述。",
            "replacement": "译注：三十年战争（1618—1648）是欧洲近代早期的重要大战，深刻改变了德意志地区的政治格局。",
        },
        "11-chapter9-fn-26": {"action": "remove", "reason": "2025 年刑事新闻时评，与原书边界不符。"},
    },
    "12-chapter10.md": {
        "12-chapter10-fn-4": {"action": "remove", "reason": "校对旁白，不纳入正式译稿。"},
    },
    "13-chapter11.md": {
        "13-chapter11-fn-1": {
            "action": "rewrite",
            "reason": "压缩为必要作品说明，移除英文残留和冗余演职员信息。",
            "replacement": "译注：《天外魔花》（Invasion of the Body Snatchers）是根据杰克·芬尼小说改编的经典科幻惊悚作品，常被用来隐喻社会同质化与身份异化。",
        },
        "13-chapter11-fn-3": {
            "action": "rewrite",
            "reason": "压缩为必要人物注释，移除近年狱中去世信息。",
            "replacement": "译注：卡利集团是哥伦比亚最重要的贩毒集团之一，曾长期主导全球可卡因贸易。",
        },
        "13-chapter11-fn-4": {
            "action": "rewrite",
            "reason": "压缩为必要宗教团体说明，移除冗长组织史细节。",
            "replacement": "译注：贵格会（Quakers）是兴起于近代英国的基督教团体，以朴素生活、诚实守信和反战立场著称。",
        },
        "13-chapter11-fn-7": {"action": "remove", "reason": "编者立场声明，不纳入主书脚注。"},
    },
}

FORBIDDEN_MAIN_PATTERNS = [
    "flushright",
    "center",
    "macin.org",
    "有点懵",
    "一个中国原则",
    "成功插入了小广告",
    "译者坚决支持",
]


def print_status(message: str) -> None:
    print(message, flush=True)


def reset_dir(path: Path) -> None:
    if path.exists():
        shutil.rmtree(path)
    path.mkdir(parents=True, exist_ok=True)


def ensure_dirs() -> None:
    REVISED_ROOT.mkdir(parents=True, exist_ok=True)
    reset_dir(SOURCE_ROOT)
    reset_dir(MAIN_ROOT)
    reset_dir(SUPPLEMENT_ROOT)
    reset_dir(REVIEW_ROOT)
    reset_dir(BUILD_ROOT)
    reset_dir(STAGING_ROOT)
    reset_dir(ASSET_ROOT)
    reset_dir(OUTPUT_ROOT)
    reset_dir(REPORT_ROOT)
    reset_dir(RAW_UNPACKED_ROOT)


def find_executable(name: str, fallbacks: list[Path] | None = None) -> str:
    path = shutil.which(name)
    if path:
        return path
    for candidate in fallbacks or []:
        if candidate.exists():
            return str(candidate)
    raise FileNotFoundError(f"missing executable: {name}")


def run_command(command: list[str], log_lines: list[str]) -> None:
    log_lines.append("$ " + " ".join(f'"{part}"' if " " in part else part for part in command))
    result = subprocess.run(
        command,
        cwd=ROOT,
        capture_output=True,
        text=True,
        encoding="utf-8",
        errors="replace",
        check=False,
    )
    if result.stdout:
        log_lines.append(result.stdout.rstrip())
    if result.stderr:
        log_lines.append(result.stderr.rstrip())
    log_lines.append("")
    if result.returncode != 0:
        raise RuntimeError(f"command failed with exit code {result.returncode}: {command[0]}")


def normalize_heading_text(text: str) -> str:
    text = re.sub(r"\s+", " ", text.strip())
    text = text.replace("： ", "：")
    text = text.replace(" : ", ": ")
    return text


def normalize_markdown(text: str) -> str:
    text = text.replace("\r\n", "\n").replace("\r", "\n")
    lines = [line.rstrip() for line in text.split("\n")]
    output: list[str] = []
    blank_count = 0
    for line in lines:
        if line.startswith("#"):
            hashes, heading = line.split(" ", 1)
            line = f"{hashes} {normalize_heading_text(heading)}"
        if line.strip():
            blank_count = 0
            output.append(line)
        else:
            blank_count += 1
            if blank_count <= 1:
                output.append("")
    return "\n".join(output).strip() + "\n"


def apply_replacements(filename: str, text: str) -> str:
    for old, new in TEXT_REPLACEMENTS.get(filename, []):
        if old in text:
            text = text.replace(old, new)
    return text


def extract_note_block(text: str, note_id: str) -> str | None:
    pattern = re.compile(rf"(?ms)^\[\^{re.escape(note_id)}\]:[^\n]*(?:\n(?:[ \t].*)?)*")
    match = pattern.search(text)
    return match.group(0).rstrip("\n") if match else None


def remove_note_block(text: str, note_id: str) -> str:
    pattern = re.compile(rf"(?ms)^\[\^{re.escape(note_id)}\]:[^\n]*(?:\n(?:[ \t].*)?)*\n?")
    return pattern.sub("", text)


def rewrite_or_remove_notes(filename: str, text: str, archived_notes: list[dict[str, str]]) -> str:
    note_actions = FOOTNOTE_ACTIONS.get(filename, {})
    for note_id, config in note_actions.items():
        original = extract_note_block(text, note_id)
        if original is None:
            continue
        archived_notes.append(
            {
                "file": filename,
                "note_id": note_id,
                "action": config["action"],
                "reason": config["reason"],
                "original": original,
            }
        )
        if config["action"] == "remove":
            text = remove_note_block(text, note_id)
            text = re.sub(rf"\[\^{re.escape(note_id)}\]", "", text)
        elif config["action"] == "rewrite":
            replacement = f"[^{note_id}]: {config['replacement']}"
            text = text.replace(original, replacement)
        else:
            raise ValueError(f"unknown footnote action: {config['action']}")
    return text


def transform_text(filename: str, text: str, archived_notes: list[dict[str, str]]) -> str:
    text = apply_replacements(filename, text)
    text = rewrite_or_remove_notes(filename, text, archived_notes)
    text = normalize_markdown(text)
    return text


def write_static_sources() -> None:
    TITLEPAGE_PATH.write_text(normalize_markdown(TITLEPAGE_TEXT), encoding="utf-8")
    IMPRINT_PATH.write_text(normalize_markdown(IMPRINT_TEXT), encoding="utf-8")
    STYLE_PATH.write_text(STYLE_TEXT, encoding="utf-8")
    METADATA_PATH.write_text(METADATA_TEXT, encoding="utf-8")


def write_main_sources() -> list[dict[str, str]]:
    archived_notes: list[dict[str, str]] = []
    for filename in COPIED_MAIN_FILES:
        source_path = LEGACY_SOURCE_ROOT / filename
        target_path = MAIN_ROOT / filename
        text = source_path.read_text(encoding="utf-8")
        transformed = transform_text(filename, text, archived_notes)
        target_path.write_text(transformed, encoding="utf-8")
    return archived_notes


def write_supplements(archived_notes: list[dict[str, str]]) -> None:
    supplement_note = dedent(
        """\
        # 补充说明

        本目录保留以下不并入主书脚注体系的材料：

        - 被直接删除的广告式、立场式、口语旁白式脚注的删除记录
        - 为维持主书注释口径而保留的处理说明

        处理原则：

        - 主书只保留原书原注、必要的极短译注和必要的人物/地名识别注
        - 私人广告、私人网站、私人政治立场、校对旁白，不在任何读者可见正文或附录材料中保留
        - 本目录只承担版本说明与删除记录功能，不视为原书正文的一部分
        """
    )
    SUPPLEMENT_NOTE_PATH.write_text(normalize_markdown(supplement_note), encoding="utf-8")

    lines = [
        "# 主书移出脚注归档",
        "",
        "以下条目已从主书与读者可见附录材料中删除，原因是其带有广告、私人立场、校对旁白或其他明显不具出版专业性的表达。",
        "",
    ]
    for item in archived_notes:
        if item["action"] != "remove":
            continue
        lines.extend(
            [
                f"## {item['file']} · {item['note_id']}",
                "",
                "- 处理方式：`delete`",
                f"- 原因：{item['reason']}",
                "",
            ]
        )
    NOTES_REMOVED_PATH.write_text("\n".join(lines), encoding="utf-8")


def concatenate_markdown(paths: list[Path]) -> str:
    return "\n\n".join(path.read_text(encoding="utf-8").strip() for path in paths) + "\n"


def write_combined_sources() -> None:
    main_paths = [MAIN_ROOT / name for name in MAIN_FILE_ORDER]
    supplement_paths = [SUPPLEMENT_NOTE_PATH, NOTES_REMOVED_PATH]
    MAIN_BOOK_PATH.write_text(concatenate_markdown(main_paths), encoding="utf-8")
    SUPPLEMENT_BOOK_PATH.write_text(concatenate_markdown(supplement_paths), encoding="utf-8")


def write_review_docs() -> None:
    review_readme = dedent(
        """\
        # 修订版审校说明

        本目录说明 `derived/revised-edition` 的版本边界与审校关闭口径。

        当前版本为“正文精修版”：

        - 主书正文在现有开源译稿基础上做忠实性修订、术语统一与书面化整理
        - 现代时评、宣传脚注与校对旁白，不再进入任何读者可见正文
        - 两份附录均作为正式附录保留在主书末尾
        - 逐章审校台账与二次复核补遗中记录的高确定性问题，已在本次修订中处理或重分类
        """
    )
    REVIEW_README_PATH.write_text(normalize_markdown(review_readme), encoding="utf-8")

    boundary = dedent(
        """\
        # 主书与补充区边界

        主书保留：

        - 书名页、版权页
        - 前言、译者序
        - 11 章正文
        - 后记
        - 附录一
        - 附录二

        补充区保留：

        - 被删除脚注的处理记录
        - 版本边界说明

        默认原则：

        - 任何带有广告、私人立场、校对旁白的内容，都不并入主书，也不并入读者可见附录
        - 需要保留的只是一条处理记录，而不是原始原文
        """
    )
    BOUNDARY_PATH.write_text(normalize_markdown(boundary), encoding="utf-8")

    closure = dedent(
        """\
        # 审校问题关闭说明

        关闭方式分三类：

        - `已改入主书`：误译、病字、排版残留、术语误判等，已直接回改
        - `已压缩重写`：原注保留必要信息，但删去无关延伸
        - `已移入补充区`：现代时评、宣传链接、立场说明、校对旁白或无原版对应内容

        对既有台账的整体处理结论：

        - `01` 至 `30`：主书正文问题已按回改规则并入修订版主书
        - `31` 至 `38`：其中涉及脚注污染、附录边界和现代增补的部分，已删除、压缩重写或改列为处理记录
        - `15-appendix.md` 与 `16-appendix2.md`：均作为正式附录保留在主书中，并继续接受术语与书面度精修

        当前版本仍保留少量“需要后续深抛光”的空间，但不再存在首轮漏章未审或显著现代污染混入主书的问题。
        """
    )
    ISSUE_CLOSURE_PATH.write_text(normalize_markdown(closure), encoding="utf-8")

    terminology = dedent(
        """\
        # 核心术语表

        本版终稿精修阶段，暂按以下口径统一核心术语：

        - `nation-state`：民族国家
        - `sovereign individual`：主权个人
        - `cognitive elite`：认知精英
        - `jurisdiction`：管辖区
        - `denationalization`：去国家化
        - `network money`：网络货币
        - `cryptocurrency`：加密货币
        - `narrowcasting`：窄播
        - `anarchy`：无政府状态
        - `anarchism`：无政府主义

        使用原则：

        - 涉及制度竞争、主权选择和税制迁移时，统一用 `管辖区`
        - 涉及信息时代的新货币形态而非具体链上资产时，优先用 `网络货币`
        - 涉及比特币、稳定币等具体对象时，再用 `加密货币`
        - `anarchy` 与 `anarchism` 严格区分，前者是状态，后者是主义
        - `denationalization` 统一译为 `去国家化`，不再与 `非国有化` 混用
        """
    )
    TERMINOLOGY_PATH.write_text(normalize_markdown(terminology), encoding="utf-8")


def render_cover() -> None:
    if ALT_COVER_SOURCE.exists():
        document = fitz.open(ALT_COVER_SOURCE)
        try:
            page = document.load_page(0)
            rect = page.rect
            target_width = rect.height * BOOK_COVER_RATIO
            x_offset = max((rect.width - target_width) / 2, 0)
            clip = fitz.Rect(x_offset, 0, x_offset + target_width, rect.height)
            pixmap = page.get_pixmap(matrix=fitz.Matrix(3, 3), clip=clip, alpha=False)
            pixmap.save(str(COVER_PATH))
        finally:
            document.close()
        return

    document = fitz.open(PDF_COVER_SOURCE)
    try:
        page = document.load_page(0)
        pixmap = page.get_pixmap(matrix=fitz.Matrix(2.2, 2.2), alpha=False)
        pixmap.save(str(COVER_PATH))
    finally:
        document.close()


def stage_main_markdown() -> list[Path]:
    staged_files: list[Path] = []
    for filename in MAIN_FILE_ORDER:
        source_path = MAIN_ROOT / filename
        target_path = STAGING_ROOT / filename
        target_path.write_text(normalize_markdown(source_path.read_text(encoding="utf-8")), encoding="utf-8")
        staged_files.append(target_path)
    return staged_files


def build_epub(pandoc: str, staged_files: list[Path], log_lines: list[str]) -> None:
    pandoc_command = [
        pandoc,
        "--from=markdown+footnotes+raw_html+fenced_divs",
        "--to=epub3",
        "--output",
        str(RAW_EPUB),
        "--css",
        str(STYLE_PATH),
        "--metadata-file",
        str(METADATA_PATH),
        "--epub-cover-image",
        str(COVER_PATH),
        "--epub-title-page=false",
        "--toc",
        "--toc-depth=2",
        "--split-level=1",
        "--standalone",
    ] + [str(path) for path in staged_files]
    run_command(pandoc_command, log_lines)


def rewrite_epub_from_dir(source_dir: Path, output_path: Path) -> None:
    with zipfile.ZipFile(output_path, "w") as archive:
        mimetype_path = source_dir / "mimetype"
        archive.write(mimetype_path, "mimetype", compress_type=zipfile.ZIP_STORED)
        for path in sorted(source_dir.rglob("*")):
            if path.is_dir() or path == mimetype_path:
                continue
            archive.write(path, path.relative_to(source_dir).as_posix(), compress_type=zipfile.ZIP_DEFLATED)


def xhtml_tag(name: str) -> str:
    return f"{{{XHTML_NS}}}{name}"


def opf_tag(name: str) -> str:
    return f"{{{OPF_NS}}}{name}"


def discover_book_paths(root_dir: Path) -> tuple[Path, Path, Path]:
    opf_candidates = list(root_dir.rglob("content.opf"))
    if not opf_candidates:
        raise FileNotFoundError("missing content.opf in extracted EPUB")
    opf_path = opf_candidates[0]
    epub_root = opf_path.parent
    text_root = epub_root / "text"
    if not text_root.exists():
        raise FileNotFoundError("missing EPUB text directory")
    return epub_root, opf_path, text_root


def get_heading_text(root: ET.Element) -> str:
    for tag_name in ("h1", "h2", "h3"):
        heading = root.find(f".//xhtml:{tag_name}", NAMESPACES)
        if heading is not None:
            text = "".join(heading.itertext()).strip()
            if text:
                return text
    raise ValueError("missing chapter heading")


def replace_note_refs(root: ET.Element) -> None:
    for anchor in root.findall(".//xhtml:a", NAMESPACES):
        href = anchor.get("href", "")
        ref_id = anchor.get("id", "")
        footnote_match = re.fullmatch(r"#fn(\d+)", href)
        ref_match = re.fullmatch(r"fnref(\d+)", ref_id)
        if footnote_match:
            note_number = footnote_match.group(1)
            anchor.set("href", f"#note-{note_number}")
            anchor.set("id", f"noteref-{note_number}")
            anchor.set("class", "note-ref")
            anchor.attrib.pop(f"{{{EPUB_NS}}}type", None)
            anchor.attrib.pop("role", None)
        elif ref_match:
            note_number = ref_match.group(1)
            anchor.set("id", f"noteref-{note_number}")
            anchor.set("class", "note-ref")
            anchor.attrib.pop(f"{{{EPUB_NS}}}type", None)
            anchor.attrib.pop("role", None)


def append_backlink(container: ET.Element, note_number: str) -> None:
    backlink = ET.Element(
        xhtml_tag("a"),
        {"href": f"#noteref-{note_number}", "class": "note-backref"},
    )
    backlink.text = "↩"
    paragraphs = [child for child in list(container) if child.tag == xhtml_tag("p")]
    target = paragraphs[-1] if paragraphs else None
    if target is None:
        target = ET.SubElement(container, xhtml_tag("p"))
    target.append(backlink)


def replace_footnote_sections(root: ET.Element) -> None:
    for section in root.findall(".//xhtml:section", NAMESPACES):
        class_name = section.get("class", "")
        if "footnotes" not in class_name.split():
            continue

        section.set("id", "chapter-notes")
        section.set("class", "chapter-notes")
        section.attrib.pop(f"{{{EPUB_NS}}}type", None)

        children = list(section)
        for child in children:
            section.remove(child)

        hr = ET.SubElement(section, xhtml_tag("hr"))
        hr.tail = "\n"
        heading = ET.SubElement(section, xhtml_tag("h2"), {"class": "notes-title"})
        heading.text = "注释"
        heading.tail = "\n"
        note_list = ET.SubElement(section, xhtml_tag("ol"), {"class": "endnotes"})
        note_list.tail = "\n"

        for child in children:
            if child.tag != xhtml_tag("aside"):
                continue

            note_id = child.get("id", "")
            match = re.fullmatch(r"fn(\d+)", note_id)
            if not match:
                continue
            note_number = match.group(1)
            note_item = ET.SubElement(note_list, xhtml_tag("li"), {"id": f"note-{note_number}"})

            moved = False
            for grandchild in list(child):
                child.remove(grandchild)
                grandchild.attrib.pop(f"{{{EPUB_NS}}}type", None)
                grandchild.attrib.pop("role", None)
                for anchor in grandchild.findall(".//xhtml:a", NAMESPACES):
                    if anchor.get("class") == "footnote-backref":
                        anchor.set("href", f"#noteref-{note_number}")
                        anchor.set("class", "note-backref")
                        anchor.attrib.pop(f"{{{EPUB_NS}}}type", None)
                        anchor.attrib.pop("role", None)
                note_item.append(grandchild)
                moved = True

            if not moved:
                paragraph = ET.SubElement(note_item, xhtml_tag("p"))
                paragraph.text = (child.text or "").strip()

            existing_backlink = note_item.find('.//xhtml:a[@class="note-backref"]', NAMESPACES)
            if existing_backlink is None:
                append_backlink(note_item, note_number)


def rewrite_titlepage_document(root: ET.Element) -> None:
    body = root.find("xhtml:body", NAMESPACES)
    if body is None:
        return

    section = body.find("xhtml:section", NAMESPACES)
    if section is None:
        return

    section.set(f"{{{EPUB_NS}}}type", "titlepage")
    section.set("id", "titlepage")
    heading = section.find("xhtml:h1", NAMESPACES)
    if heading is not None:
        heading.set("class", "tp-title")


def set_body_semantics(root: ET.Element, chapter_name: str) -> None:
    body = root.find("xhtml:body", NAMESPACES)
    if body is None:
        return
    if chapter_name in {"ch001", "ch002", "ch003", "ch004"}:
        body.set(f"{{{EPUB_NS}}}type", "frontmatter")
    else:
        body.set(f"{{{EPUB_NS}}}type", "bodymatter")


def rewrite_chapter_documents(text_root: Path) -> None:
    for xhtml_path in sorted(text_root.glob("ch*.xhtml")):
        tree = ET.parse(xhtml_path)
        root = tree.getroot()
        chapter_name = xhtml_path.stem
        title_text = "主权个人" if chapter_name == "ch001" else get_heading_text(root)

        title_element = root.find("xhtml:head/xhtml:title", NAMESPACES)
        if title_element is not None:
            title_element.text = title_text

        replace_note_refs(root)
        replace_footnote_sections(root)
        set_body_semantics(root, chapter_name)
        if chapter_name == "ch001":
            rewrite_titlepage_document(root)
        tree.write(xhtml_path, encoding="utf-8", xml_declaration=True)


def build_landmarks() -> ET.Element:
    nav = ET.Element(
        xhtml_tag("nav"),
        {f"{{{EPUB_NS}}}type": "landmarks", "id": "landmarks", "role": "doc-landmarks"},
    )
    heading = ET.SubElement(nav, xhtml_tag("h2"))
    heading.text = "位置"
    ordered = ET.SubElement(nav, xhtml_tag("ol"))
    items = [
        ("text/cover.xhtml", "封面", "cover"),
        ("text/ch001.xhtml", "书名页", "titlepage"),
        ("text/ch002.xhtml", "版权页", "copyright-page"),
        ("text/ch003.xhtml", "前言", "preface"),
        ("text/ch005.xhtml", "正文", "bodymatter"),
    ]
    for href, label, landmark_type in items:
        item = ET.SubElement(ordered, xhtml_tag("li"))
        anchor = ET.SubElement(item, xhtml_tag("a"), {"href": href, f"{{{EPUB_NS}}}type": landmark_type})
        anchor.text = label
    return nav


def rewrite_nav_document(nav_path: Path) -> None:
    tree = ET.parse(nav_path)
    root = tree.getroot()
    toc_nav = root.find('.//xhtml:nav[@epub:type="toc"]', NAMESPACES)
    if toc_nav is None:
        raise ValueError("missing toc nav in nav.xhtml")

    title_heading = toc_nav.find("xhtml:h1", NAMESPACES)
    if title_heading is not None:
        title_heading.text = "目录"

    ordered = toc_nav.find("xhtml:ol", NAMESPACES)
    if ordered is not None:
        top_items = ordered.findall("xhtml:li", NAMESPACES)
        if len(top_items) >= 1:
            first_anchor = top_items[0].find("xhtml:a", NAMESPACES)
            if first_anchor is not None:
                first_anchor.text = "书名页"
        if len(top_items) >= 2:
            second_anchor = top_items[1].find("xhtml:a", NAMESPACES)
            if second_anchor is not None:
                second_anchor.text = "版权页"

    for nav in root.findall('.//xhtml:nav[@epub:type="landmarks"]', NAMESPACES):
        parent = root.find("xhtml:body", NAMESPACES)
        if parent is not None:
            parent.remove(nav)

    body = root.find("xhtml:body", NAMESPACES)
    if body is None:
        raise ValueError("missing body in nav.xhtml")
    body.append(build_landmarks())
    tree.write(nav_path, encoding="utf-8", xml_declaration=True)


def rewrite_ncx(ncx_path: Path) -> None:
    tree = ET.parse(ncx_path)
    root = tree.getroot()
    nav_map = root.find("ncx:navMap", NAMESPACES)
    if nav_map is None:
        return
    top_points = nav_map.findall("ncx:navPoint", NAMESPACES)
    if len(top_points) >= 1:
        first_text = top_points[0].find("ncx:navLabel/ncx:text", NAMESPACES)
        if first_text is not None:
            first_text.text = "书名页"
    if len(top_points) >= 2:
        second_text = top_points[1].find("ncx:navLabel/ncx:text", NAMESPACES)
        if second_text is not None:
            second_text.text = "版权页"
    tree.write(ncx_path, encoding="utf-8", xml_declaration=True)


def rewrite_package_document(opf_path: Path) -> tuple[list[str], str | None]:
    tree = ET.parse(opf_path)
    root = tree.getroot()
    spine = root.find("opf:spine", NAMESPACES)
    if spine is None:
        raise ValueError("missing spine in content.opf")

    removed_idrefs = {"cover_xhtml", "nav"}
    kept_spine: list[str] = []
    for itemref in list(spine):
        idref = itemref.get("idref", "")
        if idref in removed_idrefs:
            spine.remove(itemref)
            continue
        kept_spine.append(idref)

    guide = root.find("opf:guide", NAMESPACES)
    if guide is None:
        guide = ET.SubElement(root, opf_tag("guide"))
    else:
        for child in list(guide):
            guide.remove(child)

    guide_entries = [
        ("cover", "封面", "text/cover.xhtml"),
        ("toc", "目录", "nav.xhtml"),
        ("title-page", "书名页", "text/ch001.xhtml"),
        ("copyright-page", "版权页", "text/ch002.xhtml"),
        ("text", "正文", "text/ch005.xhtml"),
    ]
    for entry_type, title, href in guide_entries:
        ET.SubElement(guide, opf_tag("reference"), {"type": entry_type, "title": title, "href": href})

    tree.write(opf_path, encoding="utf-8", xml_declaration=True)
    first_linear = kept_spine[0] if kept_spine else None
    return kept_spine, first_linear


def postprocess_final_epub() -> dict[str, object]:
    with zipfile.ZipFile(RAW_EPUB) as archive:
        archive.extractall(RAW_UNPACKED_ROOT)

    epub_root, opf_path, text_root = discover_book_paths(RAW_UNPACKED_ROOT)
    rewrite_chapter_documents(text_root)
    rewrite_nav_document(epub_root / "nav.xhtml")
    ncx_path = epub_root / "toc.ncx"
    if ncx_path.exists():
        rewrite_ncx(ncx_path)
    spine_items, first_linear = rewrite_package_document(opf_path)
    rewrite_epub_from_dir(RAW_UNPACKED_ROOT, FINAL_EPUB)
    return {"spine_items": spine_items, "first_linear": first_linear}


def is_ascii_path(path: str) -> bool:
    try:
        path.encode("ascii")
        return True
    except UnicodeEncodeError:
        return False


def validate_source_texts() -> dict[str, list[str]]:
    issues: list[str] = []
    for file_path in MAIN_ROOT.glob("*.md"):
        text = file_path.read_text(encoding="utf-8")
        for pattern in FORBIDDEN_MAIN_PATTERNS:
            if pattern in text:
                issues.append(f"{file_path.name}: contains forbidden token `{pattern}`")
    for file_path in [SUPPLEMENT_BOOK_PATH, NOTES_REMOVED_PATH]:
        text = file_path.read_text(encoding="utf-8")
        for pattern in FORBIDDEN_MAIN_PATTERNS:
            if pattern in text:
                issues.append(f"{file_path.name}: contains forbidden token `{pattern}`")
    return {"issues": issues}


def validate_epub() -> dict[str, object]:
    issues: list[str] = []
    nav_candidates: list[str] = []
    title_mismatches: list[str] = []
    spine_items: list[str] = []
    first_linear: str | None = None
    toc_labels: list[str] = []

    with zipfile.ZipFile(FINAL_EPUB) as archive:
        names = archive.namelist()
        if "mimetype" not in names:
            issues.append("missing mimetype")
        if "META-INF/container.xml" not in names:
            issues.append("missing META-INF/container.xml")

        for name in names:
            lower = name.lower()
            if lower.endswith("nav.xhtml"):
                nav_candidates.append(name)
            if not is_ascii_path(name):
                issues.append(f"non-ascii path: {name}")
            if lower.endswith((".xhtml", ".html", ".css", ".opf", ".ncx")):
                text = archive.read(name).decode("utf-8", errors="replace")
                if "C:\\" in text or "file://" in text:
                    issues.append(f"absolute path reference in {name}")
                if re.search(r"\\(?:chapter|section|subsection|textbf|footnote|uline)\b", text):
                    issues.append(f"LaTeX residue in {name}")
                if name.startswith("EPUB/text/") and any(
                    token in text
                    for token in ["macin.org", "有点懵", "一个中国原则", "成功插入了小广告", "译者坚决支持"]
                ):
                    issues.append(f"forbidden main-text content leaked in {name}")

        nav_name = nav_candidates[0] if nav_candidates else None
        if nav_name:
            nav_root = ET.fromstring(archive.read(nav_name))
            toc_nav = nav_root.find('.//xhtml:nav[@epub:type="toc"]', NAMESPACES)
            if toc_nav is None:
                issues.append("missing toc nav")
            else:
                toc_labels = ["".join(anchor.itertext()).strip() for anchor in toc_nav.findall(".//xhtml:a", NAMESPACES)]
                toc_links = [anchor.get("href", "") for anchor in toc_nav.findall(".//xhtml:a", NAMESPACES)]
                if not any(link.startswith("text/ch001.xhtml") for link in toc_links):
                    issues.append("title page missing from main toc")
                if not any(link.startswith("text/ch002.xhtml") for link in toc_links):
                    issues.append("imprint page missing from main toc")
                if "附录一：影响和战略" not in toc_labels:
                    issues.append("appendix one missing from main toc")
                if "附录二：实现财务独立的资源推荐" not in toc_labels:
                    issues.append("appendix two missing from main toc")

        opf_name = next((name for name in names if name.endswith("content.opf")), None)
        if opf_name:
            opf_root = ET.fromstring(archive.read(opf_name))
            spine = opf_root.find("opf:spine", NAMESPACES)
            if spine is None:
                issues.append("missing spine in content.opf")
            else:
                spine_items = [itemref.get("idref", "") for itemref in spine.findall("opf:itemref", NAMESPACES)]
                first_linear = spine_items[0] if spine_items else None
                if "nav" in spine_items:
                    issues.append("nav.xhtml still present in spine")
                if "cover_xhtml" in spine_items:
                    issues.append("cover page still present in spine")
                if first_linear != "ch001_xhtml":
                    issues.append(f"unexpected first linear chapter: {first_linear}")

        for name in names:
            if re.search(r"/text/ch\d+\.xhtml$", name):
                root = ET.fromstring(archive.read(name))
                title_element = root.find("xhtml:head/xhtml:title", NAMESPACES)
                heading_text = "主权个人" if name.endswith("/text/ch001.xhtml") else get_heading_text(root)
                title_text = (title_element.text or "").strip() if title_element is not None else ""
                if title_text != heading_text:
                    title_mismatches.append(name)

    return {
        "issues": issues,
        "nav_candidates": nav_candidates,
        "title_mismatches": title_mismatches,
        "spine_items": spine_items,
        "first_linear": first_linear,
        "toc_labels": toc_labels,
    }


def write_report(source_validation: dict[str, list[str]], epub_validation: dict[str, object]) -> None:
    lines = [
        "# 修订版 EPUB 验证报告",
        "",
        f"- Final EPUB: `{FINAL_EPUB.relative_to(ROOT).as_posix()}`",
        f"- Raw EPUB: `{RAW_EPUB.relative_to(ROOT).as_posix()}`",
        f"- Cover asset: `{COVER_PATH.relative_to(ROOT).as_posix()}`",
        "",
        "## Source Checks",
        "",
    ]
    if source_validation["issues"]:
        lines.extend(f"- {issue}" for issue in source_validation["issues"])
    else:
        lines.append("- none")

    lines.extend(
        [
            "",
            "## EPUB Checks",
            "",
            f"- `nav.xhtml` candidates: {', '.join(epub_validation['nav_candidates']) if epub_validation['nav_candidates'] else 'none'}",
            f"- First linear chapter: {epub_validation['first_linear'] or 'none'}",
            f"- Spine items: {', '.join(epub_validation['spine_items']) if epub_validation['spine_items'] else 'none'}",
            f"- TOC labels include full appendix titles: {'附录一：影响和战略' in epub_validation['toc_labels'] and '附录二：实现财务独立的资源推荐' in epub_validation['toc_labels']}",
            f"- XHTML title mismatches: {len(epub_validation['title_mismatches'])}",
            "",
            "## Issues",
            "",
        ]
    )
    all_issues = source_validation["issues"] + list(epub_validation["issues"])
    if all_issues:
        lines.extend(f"- {issue}" for issue in all_issues)
    else:
        lines.append("- none")

    REPORT_PATH.write_text("\n".join(lines) + "\n", encoding="utf-8")


def main() -> int:
    log_lines: list[str] = []
    try:
        ensure_dirs()
        write_static_sources()
        archived_notes = write_main_sources()
        write_supplements(archived_notes)
        write_combined_sources()
        write_review_docs()

        pandoc = find_executable(
            "pandoc",
            [
                Path.home() / "AppData/Local/Microsoft/WinGet/Links/pandoc.exe",
                Path.home() / "AppData/Local/Microsoft/WinGet/Packages/JohnMacFarlane.Pandoc_Microsoft.Winget.Source_8wekyb3d8bbwe/pandoc-3.9.0.2/pandoc.exe",
            ],
        )

        staged_files = stage_main_markdown()
        render_cover()
        build_epub(pandoc, staged_files, log_lines)
        postprocess_final_epub()
        source_validation = validate_source_texts()
        epub_validation = validate_epub()
        write_report(source_validation, epub_validation)
        LOG_PATH.write_text("\n".join(log_lines) + "\n", encoding="utf-8")

        print_status(f"Built: {FINAL_EPUB}")
        print_status(f"Report: {REPORT_PATH}")
        return 0 if not (source_validation["issues"] or epub_validation["issues"]) else 1
    except Exception as exc:  # noqa: BLE001
        log_lines.append(f"ERROR: {exc}")
        REPORT_PATH.write_text(f"# 修订版 EPUB 验证报告\n\n- build failed: {exc}\n", encoding="utf-8")
        LOG_PATH.write_text("\n".join(log_lines) + "\n", encoding="utf-8")
        print_status(str(exc))
        return 1


if __name__ == "__main__":
    sys.exit(main())
